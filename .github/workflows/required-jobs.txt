# The jobs `make pr.land` requires green on a PR head before it merges (D-05, L22).
# One name per line, exactly as GitHub reports it. Change this file in the same commit
# as ci.yml -- tests/test_pr_land.py fails when the two disagree.
test (3.10)
test (3.12)
vendor-bundle
image
