---
milestone: v0.1
audited: 2026-09-25T10:24:53Z
status: tech_debt
scores:
  requirements: 5/5
  phases: 4/4
  integration: 9/9
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "v0 baseline (filed 2026-09-21 at bootstrap, carried through v0.1 unchanged)"
    items:
      - "nice: CadQuery's `Shape` typing forces four `type: ignore`s"
      - "nice: No server-side cancellation on client abort"
      - "nice: No coverage floor in the gate"
      - "nice: The service has no authentication (L04, deliberate)"
      - "nice: Enji Guard not connected (owner declined)"
  - phase: "02-cad-off-the-event-loop"
    items:
      - "must: The concurrent latency bar was waived, not demonstrated (human waiver, WINDOWS.md item 1; L18 'accepted with caveat')"
  - phase: "03-structured-logging-at-the-composition-boundary (found in 03-01, defect lives in model.py's solid cache)"
    items:
      - "must: A cached solid's `.BoundingBox()` reads wrong after it has been STL-exported"
  - phase: "05-ci-observed-green"
    items:
      - "must: The commit-msg hook trusts a cut line typed by hand in an editor session (residual of 05-06; backstopped by the ruleset, pr.land's run check and D-03)"
      - "must: `make pr.land` judges a run by the listed jobs only, never by the run's own conclusion (Codex review of PR #4)"
      - "nice: The required-jobs drift test reads job ids, so a `name:` override slips past it (fails closed)"
      - "nice: `make pr.land` reports 'a skip token reached main' for any run it failed to observe"
nyquist:
  compliant_phases: ["02", "03"]
  partial_phases: []
  not_validated_phases: ["04", "05"]
  missing_phases: []
  overall: partial
security:
  verified_phases: ["02", "03", "04", "05"]
  threats_open: 0
verification_overrides:
  - phase: "02"
    item: "concurrent latency <=2.00x ratio never demonstrated on both runs of one session"
    accepted_by: "human (WINDOWS.md item 1, waived 2026-09-23)"
informational:
  - "STATE.md's CI evidence cites runs 35963114939 and 36088409707, both from before Plan 05-04 collapsed the matrix (they still show a `test (3.10)` job). Run 36122394253 on the shipped commit b72b0e1 is green with exactly the three required jobs and is the stronger citation; add it at milestone close."
---

# Milestone v0.1 "Hardening" — Audit

**Audited:** 2026-09-25T10:24:53Z against `main` = `b72b0e1` ("Phase 5: CI Observed Green (#4)").
**Verdict:** `tech_debt` — every v0.1 requirement is satisfied by three independent
sources, all four phases verified `passed`, cross-phase wiring and E2E flows complete with
no gaps; the active tech-debt ledger (11 items, 4 `must`) is the reason this is not
`passed`. Nothing blocks completion; the debt is filed with named triggers.

## Scope

Milestone v0.1 = Phases 2–5 (ROADMAP "Overview": pay down the three `must` debt items
and close the CI-unverified blocker). Phase 1 is the v0 baseline recorded for context —
built before GSD, no phase directory, no plans; its 11 requirements are `Complete (shipped
v0)` in the traceability table and were not re-verified by this audit.

Definition of done (ROADMAP): the generator is operable under real load (CAD off the
event loop, measured memory ceiling), observable (structured logging), its response
contract type-checked (typed derived dimensions), and CI is an observed, trusted merge
gate. Each maps to exactly one requirement below.

## Phase Verifications

| Phase | VERIFICATION.md | Status | Score | Notes |
|---|---|---|---|---|
| 02 CAD Off the Event Loop | 2026-09-24T05:03Z | passed | 5/5 | 1 human override on record (concurrent latency bar waived; WINDOWS.md item 1, L18). UAT complete (2 items pass). |
| 03 Structured Logging at the Composition Boundary | 2026-09-24 | passed | 4/4 | No overrides, no gaps. |
| 04 Typed Derived-Dimensions Contract | 2026-09-24T15:06Z | passed | 4/4 | No overrides, no gaps; also closed a `--mate-teeth` parity gap in shipped REQ-cli-parity. |
| 05 CI Observed Green | 2026-09-25T09:30Z | passed | 5/5 | Re-verification after 05-06 closed CR-01 (previous 4/5). No overrides. |

No phase is missing its VERIFICATION.md. Anti-pattern scans clean in all four. Human
verification pending: none. Note: `init.manager` projects all four as `stale` because
commits landed in each phase directory after its verification (ship notes, review-fix
reports; for Phase 5 the test fix `ae052f8`/`3c096de` and review fixes). Those commits
were themselves reviewed (`05-REVIEW.md` second pass, `05-REVIEW-FIX.md` all_fixed) and
CI is green 4/4 on the fixed heads; this audit treats the staleness as bookkeeping, not as
unverified behaviour — recorded so the milestone close can name its override honestly.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY `requirements-completed` | REQUIREMENTS.md | Final |
|---|---|---|---|---|---|
| REQ-cad-off-event-loop | 2 | passed — SATISFIED (truths 1a/1b/4/5) | 02-01, 02-02, 02-04, 02-05 | `[x]` / Complete | **satisfied** |
| REQ-measured-memory-ceiling | 2 | passed — SATISFIED (truths 2/3) | 02-02, 02-04, 02-05 | `[x]` / Complete | **satisfied** |
| REQ-structured-logging | 3 | passed — SATISFIED (4/4 SCs) | 03-01, 03-02, 03-03 | `[x]` / Complete | **satisfied** |
| REQ-typed-derived-dimensions | 4 | passed — SATISFIED (4/4 SCs) | 04-01, 04-02, 04-03 | `[x]` / Complete | **satisfied** |
| REQ-ci-verified | 5 | passed — SATISFIED (both halves after 05-06) | 05-01, 05-04, 05-05, 05-06 | `[x]` / Complete | **satisfied** |

Orphans: none — every traceability row for Phases 2–5 appears in its phase's
VERIFICATION.md requirements table. Plans 02-03, 05-02, 05-03 declare the requirement in
their PLAN frontmatter but list nothing in `requirements-completed` (sibling plans
completed it); no requirement rests on those alone.

Score: **5/5**.

## Cross-Phase Integration (gsd-integration-checker)

| From → To | Evidence | Status |
|---|---|---|
| Phase 2 `pool.py` → Phase 3 `records.py` (`worker_replaced`) | `pool.py:33,155`; `test_a_wedged_build_is_terminated_and_its_worker_replaced` asserts the JSON record (ERROR, cause=timeout, slot) | WIRED |
| Phase 3 `records.py` → `app.py` (build_started/failed, export_served, queue_refused, configure) | `app.py:40` + 5 call sites; `configure()` at both `cli.cmd_serve` and lifespan | WIRED |
| Phase 4 `calc.DerivedDimensions` → `/api/info` → CLI `spur info` | `test_cli_and_api_print_the_same_document` (byte-identical JSON) | WIRED |
| Phase 4 → web UI `static/app.js` | `test_every_key_the_ui_reads_is_a_derived_dimensions_field` (parses app.js, asserts ⊆ model fields) | WIRED |
| Phase 4 → OpenAPI | `test_openapi_documents_the_typed_contracts` (required lists match models) | WIRED |
| CLI `_mate_teeth` bounds ↔ `InfoQuery` | `test_info_rejects_a_mate_the_api_would_reject` reads the live schema | WIRED |
| Phase 5 `pr_land.py` → `skip_tokens.find_skip_tokens` | `pr_land.py:48,248`, reused unmodified | WIRED |
| `required-jobs.txt` ↔ `ci.yml` jobs | `test_required_jobs_file_matches_ci_yml_job_ids` (drift test) | WIRED |
| Python 3.12 only across pyproject / Makefile / ci.yml / ruleset | live run 36122394253 on b72b0e1: exactly `test (3.12)`, `vendor-bundle`, `image`, all green | WIRED |

Orphaned exports: none. Missing connections: none. Auth: not applicable (L04). Score: **9/9**.

## End-to-End Flows

| Flow | Path | Status |
|---|---|---|
| Web UI preview | `/api/schema` → form → `/api/info` → `/api/model.stl?quality=preview` → `pool.export` in a spawned worker → three.js render; records emitted along the way | COMPLETE |
| CLI ↔ API parity | `spur info` / `spur export` call the same `derive()`/`export()`; identical documents and identical validation edges proven by tests | COMPLETE |
| `/api/health` while a build is held | `queue_available` drops by exactly 1 and restores; handler is sync, reads parent-local counters only | COMPLETE |
| Timeout → worker replaced → same slot recovers | SIGTERM'd worker, `replaced` +1, `worker.replaced` record, fresh PID on the next same-slot request | COMPLETE |
| Merge gate | commit-msg hook → `make pr.land` refusals → ruleset on `main` → squash run observed; live green on b72b0e1 | COMPLETE |

Score: **5/5**. Broken flows: none.

## Nyquist Coverage (discovery only)

| Phase | VALIDATION.md | status | nyquist_compliant | Classification | Action |
|---|---|---|---|---|---|
| 02 | exists | validated | true | COMPLIANT | — |
| 03 | exists | validated | true | COMPLIANT | — |
| 04 | exists | draft | false (not authoritative) | NOT-VALIDATED | `/gsd-validate-phase 4` |
| 05 | exists | draft | false (not authoritative) | NOT-VALIDATED | `/gsd-validate-phase 5` |

Per #2117 a `draft` file was seeded by plan-phase and never reconciled by validate-phase;
it is a coverage TODO, not a compliance failure. Overall: **partial**.

## Security

All four phases carry a `SECURITY.md` with `status: verified`, `threats_open: 0`.

## Tech Debt (active ledger, `docs/tech_debt/INDEX.md`)

| Severity | Item | Origin | Trigger to revisit |
|---|---|---|---|
| must | The concurrent latency bar was waived, not demonstrated | Phase 2 (quick task 260923-qwr; human waiver) | 02-05 writes L17/L18, the harness or the machine changes, or any run reads above 2.45x |
| must | A cached solid's `.BoundingBox()` reads wrong after STL export | found in 03-01; defect in `model.py`'s solid cache | a feature needs a measured dimension from a `Solid` after export, or a test outside the autouse protection hits it |
| must | The commit-msg hook trusts a hand-typed cut line in an editor session | Phase 5 (05-06 residual) | a run-less PR head traced to a commit the hook passed; `commit.verbose` adopted; hook touched anyway |
| must | `make pr.land` never reads `run.conclusion` | Phase 5 (Codex review of PR #4) | `ci.yml` gains a job, or `scripts/pr_land.py` is touched anyway |
| nice | CadQuery `Shape` typing forces four `type: ignore`s | v0 bootstrap | CadQuery narrows its types, or the pipeline is touched |
| nice | No server-side cancellation on client abort | v0 bootstrap | slider-dragging saturates the queue |
| nice | No coverage floor in the gate | v0 bootstrap | after one measured baseline run |
| nice | The service has no authentication (L04) | v0 bootstrap | bound to anything but localhost |
| nice | Enji Guard not connected | v0 bootstrap | owner wants continuous AI audit |
| nice | Drift test ignores `jobs.<id>.name` overrides (fails closed) | Phase 5 (Codex review of PR #4) | any job gets a `name:`, or the test is touched |
| nice | `pr.land` blames a skip token for any unobserved post-merge run | Phase 5 (Codex review of PR #4) | the message prints once for real, or `pr_land.py` is touched |

Total: **11 items** (4 must, 7 nice) across the v0 bootstrap ledger and Phases 2, 3, 5.
Resolved during v0.1 (not counted): CAD builds block the event loop; no structured
logging; untyped info contract; ship-note skip token leaks into the squash merge;
test_pool wedged-worker flake on the GitHub runner.

## Informational

- STATE.md's "CI workflow unverified — resolved" note cites runs 35963114939 and
  36088409707, both pre-dating the 3.12-only matrix collapse. Run 36122394253 (push of
  `b72b0e1`, three jobs, green) is the current-shape evidence; add it at milestone close.
- Verification staleness per `init.manager` (all four phases) is explained above; the
  milestone close should record it as an override with that reason rather than re-running
  verification for bookkeeping commits.

---
_Audited by: Claude (audit-milestone orchestrator) with gsd-integration-checker (sonnet)_
