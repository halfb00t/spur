---
phase: 07-foundation-generalized-edge-selection-regression-fixture
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/spur/calc.py
  - src/spur/model.py
  - tests/test_calc.py
  - tests/test_model.py
  - docs/architecture/decision_log.md
  - docs/architecture/solid-model/tactics.md
  - bench/RESULTS.md
autonomous: true
requirements: [REQ-edge-selection-proven, REQ-defaults-off-regression]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "calc.bore_rim_limit(p) returns the exact geometric bound, the farthest any rim point can sit from the axis: bore_radius(p) for round and D-flat bores (4.575 mm for the defaults), 0.0 with no bore; no slack and no kernel import in calc.py (D-18)"
    - "model._bore_rim_edges(solid, p) selects within bore_rim_limit(p) + BORE_RIM_SLACK, a named 0.01 mm constant beside TOL whose comment carries the measured kernel tolerance and the MIN_WALL clearance; its on-rim position test is otherwise unchanged (D-18)"
    - "Both position-based selectors raise BuildError when they match zero edges, _bore_rim_edges with 'Bore chamfer selected no bore-rim edges' and _groove_floor_edges with 'Recess fillet selected no groove-floor edges', each calling it a modelling defect in spur and not a parameter conflict, so .chamfer() and .fillet() never receive an empty list (D-15, D-17)"
    - "The guard's message leaves _build_checked unchanged, not relabelled 'try smaller fillets or chamfers', and rides the existing BuildError routing (API 422, CLI exit 2) with no change to app.py, cli.py or params.py (D-15)"
    - "A test asserts the exact selected edges on the solid each selector sees before its operator runs: round bore {CIRCLE: 2}, D-flat {CIRCLE: 2, LINE: 2}, recess floor 2 per filleted floor (4 for both), over 10 rows covering both bore shapes with each recess side, no recess, no bore, and a recess at its minimum hub clearance (D-16, D-17, REQ-edge-selection-proven)"
    - "A rim bound half the bore radius selects nothing and raises BuildError through the real build path; a recess pushed to its minimum hub clearance (recess_inner_d 1.0) leaves the rim selection exact; the band stops at bore_rim_limit(p) + BORE_RIM_SLACK"
    - "A D-flat rim, an arc and a line meeting at their endpoints, selects exactly one CIRCLE and one LINE per end face; every selected CIRCLE has radius bore_radius(p) within TOL and every selected edge lies on an end face, so the recess rim circles beside it are never taken"
    - "With the feature off (bore_d 0, bore_chamfer 0, recess_sides none) no selector runs and the part is unchanged, so the fixture's records for those sets stay green; with the feature on, zero edges is a BuildError"
    - "Selector tests compare Counter multisets of geomType and counts, never the kernel's Edges() iteration order"
    - statement: "BORE_RIM_SLACK (0.01 mm) sits about five orders above the kernel's vertex and edge tolerance (1e-7 mm on every vertex and edge of five corpus gears, 2026-09-26) and 40x below MIN_WALL (0.4 mm), the least clearance recess_radii() keeps between the rim and the next end-face edge"
      verification: backstop
    - "tests/regression/pre_v0_2.json is byte-unchanged by this plan and all 85 of its cases pass after the selector change, so the change is behaviour-neutral for every pre-v0.2 set (REQ-defaults-off-regression, ROADMAP SC 3)"
    - "L26 is appended to the decision log (the fixture as the standing L05 proof and its regeneration rule; a selector never silently selects nothing); the tactics doc describes the bound and the guard; the phase's make verify cost is recorded in bench/RESULTS.md; make verify is green (ROADMAP SC 4)"
  artifacts:
    - path: "src/spur/calc.py"
      provides: "bore_rim_limit(p), the exact per-shape rim bound"
      contains: "def bore_rim_limit(p: GearParams) -> float:"
    - path: "src/spur/model.py"
      provides: "BORE_RIM_SLACK; _bore_rim_edges(solid, p) bounded by bore_rim_limit and guarded; _groove_floor_edges guarded"
      contains: "BORE_RIM_SLACK = 0.01"
    - path: "tests/test_model.py"
      provides: "The 10-row selector count and identity matrix and the two zero-edge refusal tests"
      contains: "def test_each_edge_selector_picks_exactly_its_own_edges"
    - path: "tests/test_calc.py"
      provides: "bore_rim_limit unit test"
      contains: "def test_the_bore_rim_limit_is_the_bore_radius_for_round_and_d_flat_bores"
    - path: "docs/architecture/decision_log.md"
      provides: "L26"
      contains: "## L26 — "
    - path: "docs/architecture/solid-model/tactics.md"
      provides: "Edge re-selection paragraph naming the bound and the guard"
      contains: "bore_rim_limit"
    - path: "bench/RESULTS.md"
      provides: "The phase's final measured make verify cost"
      contains: "### After the selector change (07-02)"
  key_links:
    - from: "src/spur/model.py"
      to: "src/spur/calc.py"
      via: "_bore_rim_edges computes its band from calc.bore_rim_limit(p)"
      pattern: "bore_rim_limit\\(p\\) \\+ BORE_RIM_SLACK"
    - from: "src/spur/model.py (_cut_bore)"
      to: "src/spur/model.py (_bore_rim_edges)"
      via: "the chamfer call passes p, not a pre-computed radius"
      pattern: "_bore_rim_edges\\(solid, p\\)"
    - from: "src/spur/model.py"
      to: "src/spur/build_errors.py"
      via: "both selectors raise BuildError on an empty selection"
      pattern: "selected no (bore-rim|groove-floor) edges"
    - from: "tests/test_model.py"
      to: "src/spur/model.py"
      via: "monkeypatches spur.model.bore_rim_limit and builds through _build_checked, bypassing the lru_cache"
      pattern: "spur.model.bore_rim_limit"
  prohibitions:
    - requirement_id: REQ-edge-selection-proven
      category: safety
      statement: "MUST NOT make a zero-edge selection succeed by widening, padding or retrying the selection bound, or by falling back to another selector; zero edges with the feature on is a BuildError, never a guess (D-15, D-18; ARCHITECTURE.md anti-pattern: generalizing lim by widening it)"
      status: resolved
      verification: test
---

<objective>
Generalize the bore-rim edge selector's bound into `calc.bore_rim_limit(p)` (the exact geometric
bound per bore shape, round and D-flat today, D-18), keep the 10 µm slack as a named, measured
constant in `model.py`, make both position-based selectors (`_bore_rim_edges`,
`_groove_floor_edges`) raise `BuildError` when they select nothing while their feature is on
(D-15, D-17), and prove the exact selected edges per shape with and without recesses (D-16).
The 07-01 fixture must stay byte-unchanged and green throughout. Close with L26, the tactics doc,
and the phase's measured cost.

Purpose: research Known Conflict #6: `lim = r_bore + 0.01` would select zero edges on a hex bore and
`.chamfer(d, None, [])` ships an unchamfered part with no error. Phase 8 must find the bound in
`calc.py` to extend and a guard that makes any future miss loud (L08's geometry analogue).
Output: `bore_rim_limit`, `BORE_RIM_SLACK`, two guarded selectors, 13 new tests, L26, docs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-CONTEXT.md
@.planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-PATTERNS.md
@.planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-01-SUMMARY.md
@CLAUDE.md
@docs/CODING_VALUES.md
@src/spur/model.py
@src/spur/calc.py
@src/spur/build_errors.py
@tests/test_model.py

<interfaces>
From src/spur/model.py (current, before this plan):
- `TOL = 1e-6              # mm, for matching kernel geometry back to the numbers we asked for` (line 41)
- `from .calc import Profile, bore_radius, profile, recess_fillet, recess_radii, root_fillet` (line 32)
- `def _groove_floor_edges(solid: cq.Shape, radii: tuple[float, ...], floor_z: list[float]) -> list[cq.Edge]` (lines 204-210; filters `geomType() == "CIRCLE"`, radius within TOL of a groove radius, start z within TOL of a floor height)
- `def _bore_rim_edges(solid: cq.Shape, r_bore: float, face_width: float) -> list[cq.Edge]` (lines 213-232; `on_rim` rejects edges off the end faces, then any endpoint or any of three sampled interior points beyond the bound)
- `_cut_face_recesses` fillets `_groove_floor_edges(solid, (r_in, r_out), floor_z)` only when `recess_fillet(p, rf) > 0`; `_cut_bore` chamfers `_bore_rim_edges(solid, r_bore, p.face_width)` only when `p.bore_chamfer > 0`
- `_build_checked(p)`: `except BuildError: raise`; any other exception becomes `BuildError("Geometry kernel failed (...); try smaller fillets or chamfers.")`. `build(p)` goes through `_build_cached` (lru_cache, 4).

From src/spur/calc.py:
- `MIN_WALL = 0.4`; `def bore_radius(p) -> float: return (p.bore_d + p.bore_clearance) / 2 if p.bore_d > 0 else 0.0`
- `def recess_radii(p, rf) -> tuple[float, float] | None`: hub = `r_bore + (p.bore_chamfer if p.bore_d > 0 else 0.0) + MIN_WALL`, so switching the chamfer off moves a hub-clamped recess

From src/spur/build_errors.py: `class BuildError(RuntimeError)`, routed by `app.py:414` (422) and `cli.py:111` (exit 2).

Planning probes on the pinned kernel (cadquery 2.8.0 / cadquery-ocp 7.9.3.1.1, 2026-09-26):
- Selected edges on the feature-off solid: D-flat rim {CIRCLE 2, LINE 2}, round {CIRCLE 2}, every circle radius 4.575 = bore_radius; floor 4 (both), 2 (top or bottom); all 10 matrix rows below confirmed; the 10 bare builds took 2.15 s.
- A bore-less solid with the stock recesses still yields 2 "rim" edges (the recess rim circles fall inside the band), so it is NOT a valid zero-edge provocation.
- `BRep_Tool.Tolerance_s` read 1e-7 on every vertex and edge of five corpus gears.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One shape end-to-end: calc.bore_rim_limit(p) bounds the bore-rim selector, an empty selection raises, proven on the stock D-flat gear and the unchanged fixture</name>
  <files>src/spur/calc.py, src/spur/model.py, tests/test_calc.py, tests/test_model.py</files>
  <read_first>
    - src/spur/model.py (whole file; lines 159-233 and 237-259 especially)
    - src/spur/calc.py (lines 1-83: MIN_WALL, bore_radius, recess_radii)
    - src/spur/build_errors.py
    - tests/test_model.py and tests/test_calc.py (lines 1-40): imports, parametrize and approx idioms
    - tests/regression/test_pre_v0_2.py (07-01's replay test: must stay green and unmodified)
    - .planning/research/ARCHITECTURE.md (lines 127-206, Q2; lines 438-448, the widening anti-pattern)
    - .planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-CONTEXT.md (D-15..D-18, `<specifics>`)
    - .planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-PATTERNS.md (model.py section: TOL's comment shape, the guard pattern)
    - docs/tech_debt/active/2026-09-21-cadquery-shape-typing.md (why the chamfer call keeps its type: ignore)
  </read_first>
  <action>
If this is a new session, run `make verify` once first (D-20), and confirm `make test PYTEST_ARGS="tests/regression -q"` reads 85 passed before any edit.

Measure before writing the constant's comment: with `.venv/bin/python`, build `GearParams(bore_chamfer=0)` and `GearParams(bore_flat=0, bore_chamfer=0)` through `spur.model._build_checked` and read the largest `OCP.BRep.BRep_Tool.Tolerance_s(x.wrapped)` over `Vertices()` and over `Edges()`. Planning read 1e-7 mm everywhere on five corpus gears (2026-09-26); write the number you read.

`src/spur/calc.py`, per D-18: add `def bore_rim_limit(p: GearParams) -> float:` directly below `bore_radius`, returning `bore_radius(p)` (so 0.0 with no bore). Docstring: the farthest any point on the bore's rim can sit from the axis, the exact geometric bound with no slack; round and D-flat both reduce to `bore_radius(p)` because the D-flat hole is the round hole intersected with a rectangle (`model._cut_bore`), a strict subset of the circle; it is not the bore's characteristic radius once a shape has vertices outside that circle, and Phase 8 adds the hex circumradius (across-flats over sqrt 3) here instead of widening the band (research ARCHITECTURE.md Q2 and its anti-pattern); `calc.py` knows no kernel tolerance, the selection slack belongs to `model.py`. No new import.

`src/spur/model.py`, per D-15, D-16, D-18: add `bore_rim_limit` to the existing `from .calc import ...` line, alphabetical (`Profile, bore_radius, bore_rim_limit, profile, recess_fillet, recess_radii, root_fillet`). Add `BORE_RIM_SLACK = 0.01` directly below `TOL` with a comment in TOL's shape: mm; how far past `bore_rim_limit()` a rim point may read and still count; why 10 µm and not `TOL`: it must clear the kernel's post-boolean vertex tolerance (the number you measured) by orders of magnitude and stay far below `MIN_WALL` (0.4 mm), the least clearance `recess_radii()` keeps between the rim and the next end-face edge (continue on a second comment line if it passes 100 columns). Change the selector to `def _bore_rim_edges(solid: cq.Shape, p: GearParams) -> list[cq.Edge]:`; replace its inline 10 µm bound with `lim = bore_rim_limit(p) + BORE_RIM_SLACK`; `on_rim` uses `p.face_width` and is otherwise unchanged (D-18: only where the bound comes from changes, not the selection logic). Collect the matching edges; if the list is empty, raise `BuildError("Bore chamfer selected no bore-rim edges: a modelling defect in spur, not a conflict in these parameters. Set bore_chamfer to 0 to build this gear without it.")`; else return it. The docstring keeps "selected by position, not by type" and the recess-separation invariant, and adds that the band comes from `calc.bore_rim_limit(p)` and that this selector runs only with `bore_chamfer > 0`, so an empty result is a defect, never an answer (D-15). In `_cut_bore`, call `_bore_rim_edges(solid, p)`; keep `r_bore` for the hole and keep the chamfer call's existing `# type: ignore[attr-defined]` comment.

`tests/test_calc.py`: add `test_the_bore_rim_limit_is_the_bore_radius_for_round_and_d_flat_bores`: for `GearParams(bore_flat=0)`, `GearParams()` and `GearParams(bore_d=0)`, `bore_rim_limit(p) == bore_radius(p)` exactly, and the values are `pytest.approx(4.575)`, `pytest.approx(4.575)` and `0.0`.

`tests/test_model.py`: add `test_each_edge_selector_picks_exactly_its_own_edges(kw, rim, floor)`, parametrized, with ONE row in this task: id `d-flat-both`, kw `{}`, rim `Counter({"CIRCLE": 2, "LINE": 2})`, floor 4 (Task 2 adds the other nine). Body: `bare_p = GearParams.model_validate({**kw, "bore_chamfer": 0, "recess_fillet": 0})` and `bare = build(bare_p)`, the solid each selector sees in the pipeline just before its operator runs (the chamfer is the last step; the recess fillet only adds faces away from the rim). Every selector argument comes from `bare_p`, not from the as-requested params: `bore_chamfer` enters `recess_radii()`'s hub clearance, so a hub-clamped recess moves when the chamfer is switched off (5.375 to 4.975 mm at `recess_inner_d` 1.0, planning probe) and radii taken from the chamfered params would miss the bare solid's floor circles. Rim: `Counter(e.geomType() for e in _bore_rim_edges(bare, bare_p))` when `bare_p.bore_d > 0`, else `None`. Floor: `rr = recess_radii(bare_p, profile(bare_p).rf)`; heights `bare_p.recess_depth` for `both`/`bottom` and `bare_p.face_width - bare_p.recess_depth` for `both`/`top`; `len(_groove_floor_edges(bare, rr, heights))` when `rr` is not `None`, else `None`. Assert `(rim_counter, floor_count) == (rim, floor)` as one tuple, so a wrong floor count never hides behind a wrong rim count. Then identity: every selected rim edge's start and end z within `TOL` of 0 or `face_width`, and every selected `CIRCLE`'s `radius()` within `TOL` of `bore_radius(bare_p)`. Compare multisets and counts, never the `Edges()` order. (This asserts a selector's result, which REQ-edge-selection-proven requires; it spies on no call, which CODING_VALUES bans.)

Run the verify commands. `git diff --exit-code tests/regression/pre_v0_2.json` must pass: the fixture byte-unchanged and green is the proof the new bound is behaviour-neutral for every pre-v0.2 set. Commit the four paths by name, plain `git commit` (D-20): `feat(07-02): bound the bore-rim selector by calc.bore_rim_limit and refuse an empty selection`.
  </action>
  <verify>
    <automated>make test PYTEST_ARGS="tests/regression tests/test_calc.py tests/test_model.py -q" && git diff --exit-code tests/regression/pre_v0_2.json</automated>
    <fails_when>non-zero exit, "failed" in the pytest summary line, or git diff prints any change to the fixture</fails_when>
    <automated>make lint typecheck lint-imports</automated>
    <fails_when>non-zero exit, or lint-imports reports a broken contract</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '^def bore_rim_limit(p: GearParams) -> float:' src/spur/calc.py` prints 1
    - `grep -c '^BORE_RIM_SLACK = 0\.01 ' src/spur/model.py` prints 1
    - `grep -c 'def _bore_rim_edges(solid: cq.Shape, p: GearParams) -> list\[cq.Edge\]:' src/spur/model.py` prints 1
    - `grep -c 'lim = bore_rim_limit(p) + BORE_RIM_SLACK' src/spur/model.py` prints 1, and `grep -c '_bore_rim_edges(solid, p)' src/spur/model.py` prints 1
    - `grep -cE 'r_bore \+ 0\.01' src/spur/model.py` prints 0
    - `grep -c 'selected no bore-rim edges' src/spur/model.py` prints 1
    - `git diff --exit-code tests/regression/pre_v0_2.json` exits 0
    - `make lint-imports` reports "The gear maths stays free of the CAD kernel" KEPT
  </acceptance_criteria>
  <done>`bore_rim_limit` sets the selector's band, `BORE_RIM_SLACK` carries its measured reason, an empty bore-rim selection raises `BuildError`, the stock gear's rim and floor selections are asserted exactly, and the unchanged fixture still passes all 85 cases.</done>
</task>

<task type="auto">
  <name>Task 2: Both selectors guarded and count-tested for every bore shape, with and without recesses, plus the two zero-edge refusals</name>
  <files>src/spur/model.py, tests/test_model.py</files>
  <read_first>
    - src/spur/model.py (as Task 1 left it; `_groove_floor_edges` and `_bore_rim_edges`)
    - tests/test_model.py (as Task 1 left it)
    - .planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-CONTEXT.md (D-15, D-16, D-17)
    - .planning/research/PITFALLS.md (lines 168-235, Pitfall 4: count and identity, re-audit when a feature shares the face)
    - tests/test_pool.py (lines 230-275: the precedent for testing a BuildError by its text)
  </read_first>
  <action>
`src/spur/model.py`, per D-15 and D-17: `_groove_floor_edges` keeps its signature and its filter; collect the matching edges; if the list is empty, raise `BuildError("Recess fillet selected no groove-floor edges: a modelling defect in spur, not a conflict in these parameters. Set recess_fillet to 0 to build this gear without it.")`; else return it. Docstring: the separating invariant (a `CIRCLE` whose radius is within `TOL` of a groove radius and whose start point sits within `TOL` of a floor height); it runs only when the recess fillet is above zero, so an empty result is a defect (D-15); Phase 11's cutouts put new circles on the recessed floor and must re-prove this invariant (Pitfall 4).

`tests/test_model.py`: extend `test_each_edge_selector_picks_exactly_its_own_edges` to ten rows, each with the given `id` (all ten confirmed on the pinned kernel at planning, 2026-09-26; rim written as a Counter, `None` where there is no bore; floor as a count, `None` where there is no recess):
- `d-flat-both`: `{}`; CIRCLE 2 + LINE 2; 4
- `d-flat-top`: `{"recess_sides": "top"}`; CIRCLE 2 + LINE 2; 2
- `d-flat-bottom`: `{"recess_sides": "bottom"}`; CIRCLE 2 + LINE 2; 2
- `d-flat-no-recess`: `{"recess_sides": "none"}`; CIRCLE 2 + LINE 2; None
- `round-both`: `{"bore_flat": 0}`; CIRCLE 2; 4
- `round-top`: `{"bore_flat": 0, "recess_sides": "top"}`; CIRCLE 2; 2
- `round-bottom`: `{"bore_flat": 0, "recess_sides": "bottom"}`; CIRCLE 2; 2
- `round-no-recess`: `{"bore_flat": 0, "recess_sides": "none"}`; CIRCLE 2; None
- `no-bore`: `{"bore_d": 0}`; None; 4
- `d-flat-recess-at-hub-clearance`: `{"recess_inner_d": 1.0}`; CIRCLE 2 + LINE 2; 4

The last row is the boundary: `recess_inner_d` 1.0 clamps the recess to its minimum hub clearance (`bore_radius + MIN_WALL` on the chamfer-off solid, 4.975 mm), the closest a recess edge can come to the rim band, and the rim selection stays exact.

Add two refusal tests (D-15):
- `test_a_bore_chamfer_that_selects_no_rim_edges_is_a_build_error_not_a_bare_bore(monkeypatch)`: `monkeypatch.setattr("spur.model.bore_rim_limit", lambda p: bore_radius(p) / 2)`, a bound inside the bore and the Phase 8 hex failure in miniature (a bound smaller than the rim's farthest point). `_build_checked(GearParams())` raises `BuildError` matching `selected no bore-rim edges`, and the message does not contain `try smaller` (the catch-all did not relabel it). Call `_build_checked`, never `build()`: `build()`'s `lru_cache` would hand back a solid built before the patch, without running the selector at all. Do not provoke this with a bore-less solid: with the stock recesses the band then picks up the recess rim circles (2 edges for `bore_d` 0, planning probe), so it is not empty.
- `test_a_recess_fillet_that_selects_no_floor_edges_is_a_build_error()`: take the stock groove radii `recess_radii(GearParams(), profile(GearParams()).rf)` and floor heights `[2.0, 5.5]` (the stock `recess_depth` and `face_width - recess_depth`), and call `_groove_floor_edges` on `build(GearParams(recess_sides="none"))`, a solid with no groove: it raises `BuildError` matching `selected no groove-floor edges`.

Run the verify commands; the fixture must stay byte-unchanged and green. Commit the two paths by name (D-20): `feat(07-02): refuse an empty recess-floor selection and count both selectors per bore shape`.
  </action>
  <verify>
    <automated>make test PYTEST_ARGS="tests/test_model.py tests/regression -q" && git diff --exit-code tests/regression/pre_v0_2.json</automated>
    <fails_when>non-zero exit, "failed" or "error" in the pytest summary line, or git diff prints a change to the fixture</fails_when>
    <automated>.venv/bin/python -m pytest tests/test_model.py --collect-only -q | grep -c 'test_each_edge_selector_picks_exactly_its_own_edges\['</automated>
    <fails_when>prints anything other than 10</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'selected no groove-floor edges' src/spur/model.py` prints 1
    - `grep -c 'raise BuildError(' src/spur/model.py` prints 4 (the two existing kernel-failure raises plus the two guards)
    - `make test PYTEST_ARGS="tests/test_model.py -q -k selects_no"` reports "2 passed"
    - `grep -c 'spur.model.bore_rim_limit' tests/test_model.py` prints 1
    - `git diff --exit-code tests/regression/pre_v0_2.json` exits 0
  </acceptance_criteria>
  <done>Both selectors refuse an empty selection with their own message; ten rows prove the exact selected edges per bore shape with and without recesses, with no bore, and at the recess's minimum hub clearance; the two refusals are proven through the real paths; the fixture is unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 3: Log L26, keep the tactics doc true, record the phase's measured cost, run the full gate</name>
  <files>docs/architecture/decision_log.md, docs/architecture/solid-model/tactics.md, bench/RESULTS.md</files>
  <read_first>
    - docs/architecture/decision_log.md (lines 1-12, the append-only rule; lines 608-760, L24 and L25: the entry shape)
    - docs/architecture/solid-model/tactics.md (lines 1-45)
    - bench/RESULTS.md (07-01's `## Regression fixture cost (Phase 7, D-06)` section)
    - .planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-01-SUMMARY.md (the measured delta, the D-06 outcome, the tripwire split, the provenance values)
    - .planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-CONTEXT.md (every "Rejected" clause in D-01..D-18; `<deferred>`)
  </read_first>
  <action>
`docs/architecture/decision_log.md` is append-only: add `## L26 — The pre-v0.2 part is pinned by a regression fixture, and an edge selector never silently selects nothing` after L25 and change nothing above it (CONTEXT's Claude's-discretion item, taken: one entry covering D-03 and D-15). Follow L24/L25's shape: `Date: <today>.`, then bold-led paragraphs:
- **The fixture.** `tests/regression/pre_v0_2.json` pins every pre-v0.2 hand-written parameter set (78 source-tagged entries from `tests/` and `README.md`, 44 records: 39 built, 5 mate-only): `derive()`'s 19 fields exactly, warning text included; face and edge counts exactly; `Volume()` within rel=1e-6; six `BoundingBox()` corners within abs=1e-6. Give each tolerance the number that set it: Volume() identical over three builds; the bore chamfer is 1.05e-3 and the recess fillets 2.9e-3 of the default volume (why rel=1e-3 was rejected); the box's ~1e-7 padding; 172/490 against 168/482 faces/edges with and without the chamfer. Export bytes are not compared (L24). Its measured make verify cost and the tripwire split, from bench/RESULTS.md and 07-01-SUMMARY.md; if the D-06 checkpoint fired, the option the human chose and the date.
- **The regeneration rule** (D-03): only `make fixture.regen`, only in its own commit that says what moved and why, a `cadquery`/`cadquery-ocp` pin bump (L12) or a deliberate contract change carrying its own Lxx; never in a feature commit; a red fixture in Phases 8-12 is that phase's bug. Name the kernel-version test and the `must` debt item on CI's unpinned kernel.
- **The selector rule** (D-15..D-18): `calc.bore_rim_limit(p)` is the exact bound per bore shape; `model.BORE_RIM_SLACK` (0.01 mm) with the measured kernel tolerance from Task 1; both selectors raise `BuildError` on zero edges (quote both messages); the tests assert exact counts and identity per shape; the runtime guard is non-empty only.
- **Rejected.** Python literals in the test module and a generated module (D-01); a pytest flag that regenerates, and capture by hand (D-02); frozen for the milestone, and regenerate-whenever-red (D-03); fully expanded params (D-04); harvesting by instrumentation and importing test parametrize lists (D-05); a curated build subset and a CI-only marker (D-06); rel=1e-3 and exact float volume (D-11); three extents and a relative bbox tolerance (D-12); volume and bbox only (D-13); one looping test (D-14); letting the catch-all relabel the defect, an `EdgeSelectionError` subclass (deferred), HTTP 500 (D-15); a runtime exact-count rule (D-16); bore rim only (D-17); slack inside `bore_rim_limit`, and positive identification (deferred) (D-18).
- **Reversibility.** Reversible: `tests/regression/` is local to the tests and the guards are two checks in `model.py`; the regeneration rule changes only by a superseding entry.
- **Reason:** the regressions it exists to catch: a new cut step touching an old part, a reworded warning, a drifted default, a kernel bump, and a bound too small for a new bore shape (the hex case, research SUMMARY.md Known Conflict #6).

`docs/architecture/solid-model/tactics.md`, surgical: in the **Edge re-selection** paragraph, say `_bore_rim_edges()` matches by position within `calc.bore_rim_limit(p)` plus `BORE_RIM_SLACK`, and that both selectors raise `BuildError` when they match nothing while their feature is on (L26). In **Out:**, extend the `BuildError` bullet: the two edge selectors raise it themselves on an empty selection. No other wording changes.

`bench/RESULTS.md`: under 07-01's section add `### After the selector change (07-02)`: host-state line (`uptime` load averages); two `time make verify` wall-clock runs with their pytest summary lines (these two runs are also this task's gate); the selector tests' durations from `make test PYTEST_ARGS="tests/test_model.py tests/test_calc.py -q --durations=15"`; the phase's total pytest delta against the 191 tests / 32.47 s at `b3ca789` in CONTEXT.

Commit the three paths by name (D-20): `docs(07-02): log L26 and record the phase's measured make verify cost`. Landing is `/gsd-ship` and then `make pr.land PR=N` after verification (D-19, L22/L25), not a task in this plan.
  </action>
  <verify>
    <automated>grep -c '^## L26 — ' docs/architecture/decision_log.md</automated>
    <fails_when>prints anything other than 1</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit, or "failed" in pytest's summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Before committing, `git diff --numstat -- docs/architecture/decision_log.md` reports 0 deleted lines (append-only), and `grep -cE '^## L2[0-5] — ' docs/architecture/decision_log.md` prints 6
    - `grep -c 'bore_rim_limit' docs/architecture/solid-model/tactics.md` prints at least 1, and `grep -c 'L26' docs/architecture/solid-model/tactics.md` prints at least 1
    - `grep -c '^### After the selector change (07-02)' bench/RESULTS.md` prints 1
    - `git diff --quiet b3ca789 -- src/spur/app.py src/spur/cli.py src/spur/params.py` exits 0 (no routing, CLI or parameter change in this phase)
    - `make verify` exits 0 (expected 289 passed: 276 after 07-01, plus 1 calc test, 10 matrix rows and 2 refusal tests)
  </acceptance_criteria>
  <done>L26 records the fixture, its regeneration rule and the selector guard with the numbers behind each; tactics.md describes the bound and the guard; bench/RESULTS.md holds the phase's final measured cost; `make verify` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to build pipeline | API query and CLI flags reach `_build` only as a validated, frozen `GearParams`; this plan adds no field and no parsing |
| build pipeline to client | a `BuildError` message becomes a 422 `detail` (API) or stderr (CLI) |
| keystroke path | `calc.py` runs on every form change and must stay free of the kernel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | `_bore_rim_edges` / `_groove_floor_edges` feeding `.chamfer()` / `.fillet()` (integrity of the part a user prints) | medium | mitigate | `BuildError` on an empty selection inside both selectors; the 10-row exact count and identity matrix; the 07-01 fixture byte-unchanged and green (its tripwire goes red on a vanished chamfer) |
| T-07-07 | Information disclosure | guard message, reaching the API 422 body and CLI stderr | low | mitigate | Static text naming only the feature and the parameter to zero; no exception repr, file path or kernel type is interpolated |
| T-07-08 | Denial of service | per-request cost of the guard | low | accept | One emptiness check on a list the selector already built; no extra kernel call |
| T-07-09 | Tampering | `calc.py` purity | low | mitigate | `bore_rim_limit` adds no import; `make lint-imports`' contract "The gear maths stays free of the CAD kernel" gates it (Task 1 acceptance) |
| T-07-SC | Tampering | package installs | low | accept | This plan installs no package and changes no dependency |
</threat_model>

<verification>
- `make test PYTEST_ARGS="tests/regression tests/test_calc.py tests/test_model.py -q"`: no failures
- `git diff --exit-code tests/regression/pre_v0_2.json` after every task (fixture untouched, green)
- `.venv/bin/python -m pytest tests/test_model.py --collect-only -q` shows 10 matrix cases
- `make lint-imports`: calc.py stays kernel-free
- `make verify` green (expected 289 passed)
</verification>

<success_criteria>
- ROADMAP Phase 7 SC 1: `bore_rim_limit(p)` computes the bound per bore shape (round and D-flat), shaped for Phase 8's hex; a test asserts the selection is non-empty, and exact, whenever `bore_chamfer > 0`, for every bore shape that exists
- ROADMAP Phase 7 SC 2: a selector that finds zero edges while its feature is on raises `BuildError` (both selectors)
- ROADMAP Phase 7 SC 3 (this plan's share): the 07-01 fixture passes unmodified after the selector change
- ROADMAP Phase 7 SC 4: `make verify` green with the fixture and the selector tests
- REQ-edge-selection-proven: count per bore profile with and without recesses; build error on zero selection (cutout patterns arrive in Phase 11 and extend the matrix there)
</success_criteria>

<output>
Create `.planning/phases/07-foundation-generalized-edge-selection-regression-fixture/07-02-SUMMARY.md` when done. Record: the measured kernel tolerance written into `BORE_RIM_SLACK`'s comment, the two `make verify` wall times, and confirmation that `tests/regression/pre_v0_2.json` never changed in this plan.
</output>

## Artifacts this phase produces (07-02)

| Artifact | Kind | Notes |
|----------|------|-------|
| `spur.calc.bore_rim_limit(p: GearParams) -> float` | new function | exact rim bound per bore shape; Phase 8 adds hex here |
| `spur.model.BORE_RIM_SLACK` | new constant | 0.01 mm, beside `TOL`, with its measured reason |
| `spur.model._bore_rim_edges(solid: cq.Shape, p: GearParams) -> list[cq.Edge]` | changed signature | replaces `(solid, r_bore, face_width)`; raises `BuildError` on an empty selection |
| `spur.model._groove_floor_edges` | changed behaviour | same signature; raises `BuildError` on an empty selection |
| `BuildError` message "Bore chamfer selected no bore-rim edges: ..." | new text | full text in Task 1 |
| `BuildError` message "Recess fillet selected no groove-floor edges: ..." | new text | full text in Task 2 |
| `tests/test_calc.py::test_the_bore_rim_limit_is_the_bore_radius_for_round_and_d_flat_bores` | new test | |
| `tests/test_model.py::test_each_edge_selector_picks_exactly_its_own_edges` | new test | 10 parametrized rows |
| `tests/test_model.py::test_a_bore_chamfer_that_selects_no_rim_edges_is_a_build_error_not_a_bare_bore` | new test | monkeypatches `spur.model.bore_rim_limit` |
| `tests/test_model.py::test_a_recess_fillet_that_selects_no_floor_edges_is_a_build_error` | new test | |
| `docs/architecture/decision_log.md` L26 | new decision entry | |
| `docs/architecture/solid-model/tactics.md` | edit | Edge re-selection and Out paragraphs |
| `bench/RESULTS.md` `### After the selector change (07-02)` | new subsection | |

## Edge coverage (spec-less probe fallback, REQ-edge-selection-proven)

| Probe | Resolution | Where |
|-------|------------|-------|
| boundary (at the threshold, one step either side) | resolved, explicit: a half-radius bound raises; a recess at minimum hub clearance keeps the count exact | must_haves.truths; Task 2 rows and refusal test |
| adjacency (touching edges) | resolved, explicit: the D-flat arc and line both selected, circle radii equal bore_radius, recess circles excluded | must_haves.truths; Task 1 identity check |
| empty (zero selection) | resolved, explicit: `BuildError` with the feature on; the selector never runs with it off | must_haves.truths; Task 2 refusal tests; 07-01 fixture records |
| ordering (equal elements) | resolved, explicit: Counter multisets, never `Edges()` order | must_haves.truths; Task 1 test body |
| precision (tolerance contract) | resolved, backstop: the slack's margin over kernel tolerance and under MIN_WALL | must_haves.truths `{statement, verification: backstop}`; Task 1 measurement |

Coverage equality across both plans: 8 probe items surfaced, 8 authored (7 explicit, 1 backstop), 0 unresolved.

## Flagged Assumptions

- **A7.** The guard lives inside each selector, not at the call sites. Both run only with their feature on (`bore_chamfer > 0`, recess fillet above 0), so an empty result is always a defect, and Phases 8 and 9 inherit the guard just by reusing the selector.
- **A8.** The count tests call the private selectors directly on a feature-off solid. That follows the precedent of tests already importing `_build_checked` and `bench.memory._is_capped`, and it is how the selectors' results can be asserted, which REQ-edge-selection-proven requires. Spying on the pipeline's call would instead assert a private call, which CODING_VALUES bans.
- **A9.** The guard messages suggest zeroing the feature as the workaround. L15: an error names the parameter and what to change. The workaround is honest, because with the feature off the defective selector never runs.

Source audit: phase-wide table in 07-01-PLAN.md § Source Coverage Audit. This plan owns REQ-edge-selection-proven, ARCH Q2, PITFALLS 4, SUMMARY KC#6, D-15..D-18, D-03's L26 entry, and D-20 for its own commits.
