---
phase: 05-ci-observed-green
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - docs/architecture/decision_log.md
  - pyproject.toml
  - src/spur/params.py
  - src/spur/pool.py
  - src/spur/records.py
  - Makefile
  - .github/workflows/ci.yml
  - .github/workflows/required-jobs.txt
  - src/spur/build_errors.py
  - tests/test_pool.py
autonomous: true
requirements: [REQ-ci-verified]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/architecture/decision_log.md` gains `## L23 — Python 3.12 only (supersedes L01's floor)`, appended after L22 and dated 2026-09-25. It quotes L01's own words 'detected, not chosen', keeps L01's ceiling reasoning (no `cadquery-ocp` wheels past 3.12), cites the incident (`logging.StreamHandler[TextIO]`; runs 35993984796, 36028253714, 36028759311; fixed `990d1fe`), and lists the 3.12-only constructs this plan adopts as the unwind list for any future widening (D-08, D-09)"
    - "`pyproject.toml` says `requires-python = \">=3.12,<3.13\"` and ruff `target-version = \"py312\"`; mypy's `python_version = \"3.12\"` is unchanged, and its comment keeps the numpy-stub reason and no longer claims the floor is checked by a real 3.10 run (D-09, ROADMAP SC-3)"
    - "`.github/workflows/ci.yml` runs the `test` job on `[\"3.12\"]` only, still reported as `test (3.12)`, and `.github/workflows/required-jobs.txt` loses the 3.10 job in the same commit, so the drift test stays green (D-05, D-09)"
    - "After the collapse, the required checks of the ruleset on `main`, read back live from `gh api repos/halfb00t/spur/rules/branches/main`, equal the non-comment lines of `required-jobs.txt` exactly -- the wall, the tool's list and `ci.yml` name the same three jobs, and none of them names a job no workflow reports (D-12, ROADMAP SC-5)"
    - "The Makefile picks `python3.12` and nothing else, and with no `python3.12` it refuses with `make: no python3.12 on PATH.` (D-09)"
    - "`ruff check .` is clean at the new target. At planning, `ruff check --target-version py312 .` reported exactly three findings: UP047 at `src/spur/params.py:19` (`_f`'s TypeVar), UP041 at `src/spur/pool.py:184` (the qualified asyncio TimeoutError), and UP017 at `src/spur/records.py:67` (`datetime.timezone.utc`). Each is fixed, along with anything the new target reports in `scripts/`"
    - "No code comment still reasons from a 3.10 floor or from CI running a second version: `build_errors.py`'s docstring, `pool.py`'s except comment, `records.py`'s handler base and `_parse_level` docstring, and `tests/test_pool.py`'s `_processes` docstring all state the 3.12-only reality, and `records._JsonHandler` subclasses `logging.StreamHandler[TextIO]` directly -- the type-checking-only indirection existed solely for 3.10"
    - "`make verify` passes on the new target, including the pip reinstall `pyproject.toml`'s change triggers (L13)"
  prohibitions:
    - statement: "L01 is not edited. It is superseded on its floor by L23's heading and body, and its text, like every earlier entry, stays byte-identical (D-08). Backed by the diff check in Task 1."
      status: resolved
      verification: test
  artifacts:
    - path: "docs/architecture/decision_log.md"
      provides: "L23, superseding L01's floor"
      contains: "## L23"
    - path: "pyproject.toml"
      provides: "The one supported interpreter, checkable by pip"
      contains: "requires-python = \">=3.12,<3.13\""
    - path: ".github/workflows/ci.yml"
      provides: "The one-entry matrix"
      contains: "python: [\"3.12\"]"
    - path: ".github/workflows/required-jobs.txt"
      provides: "The required job set without the 3.10 leg"
      contains: "test (3.12)"
    - path: "Makefile"
      provides: "`make venv` choosing python3.12 only"
      contains: "for p in python3.12; do"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows/required-jobs.txt"
      via: "the matrix and the required list change in one commit; tests/test_pr_land.py's drift test holds them equal"
      pattern: "test (3.12)"
    - from: "pyproject.toml"
      to: "docs/architecture/decision_log.md"
      via: "the ruff and mypy comments cite L23"
      pattern: "L23"
---

<objective>
spur supports Python 3.12 only (D-09), and the decision is on file (D-08).

**Task 1.** `L23` supersedes L01's floor. The floor was "detected, not chosen". mypy
cannot hold a floor below 3.12, and the only 3.10 execution this code ever had was CI's
leg. That gap let a 3.11-only construct reach `main`, unimportable on 3.10. With one
version, the type checker's version is the runtime.

**Task 2.** The package says it: `requires-python` gains an upper bound, so pip refuses
instead of spending minutes on OpenCascade. Ruff targets py312, and the three findings
that target raises are fixed. `records.py` drops the 3.10-only workaround.

**Task 3.** The build says it: `make venv` picks `python3.12`, and CI runs one matrix
entry. `pr.land`'s required list loses the 3.10 job in the same commit (D-05). The
comments that argued from the old floor are made true.

Purpose: the supported Python is the one that actually runs, and the class of bug that
reached `main` through the 3.10 gap cannot come back without a new decision.

Output: L23; `pyproject.toml`, `Makefile`, `ci.yml`, `required-jobs.txt` agreeing on
3.12; the three ruff-required changes; code comments that are true.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-observed-green/05-CONTEXT.md
@.planning/phases/05-ci-observed-green/05-RESEARCH.md
@.planning/phases/05-ci-observed-green/05-02-SUMMARY.md
@CLAUDE.md
@docs/CODING_VALUES.md
</context>

<artifacts_this_phase_produces>
This plan creates:

- `## L23` in `docs/architecture/decision_log.md`
- `pyproject.toml`: `requires-python = ">=3.12,<3.13"`, `target-version = "py312"`
- `spur.params._f` re-declared with a PEP 695 type parameter (no module-level `T`)
- `Makefile`: `PYTHON ?=` loop over `python3.12` only; error text `make: no python3.12 on PATH.`
- `.github/workflows/ci.yml`: `python: ["3.12"]`
- `.github/workflows/required-jobs.txt`: three jobs

Rest of the phase: `05-01` (`scripts/skip_tokens.py`, the hook), `05-02`
(`scripts/pr_land.py`, `make pr.land`), `05-03` (the ruleset on `main`, `L22`, section
8), `05-05` (the prose and the record made true).
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **D-09's footprint is larger than CONTEXT listed; everything added is forced.**
   Found during planning with `ruff check --target-version py312 .`. The project selects
   ruff's `UP` rules ("modern syntax for the lowest supported Python"), and at py312
   they report three findings, so `make verify` fails until they are fixed:
   - UP047 `src/spur/params.py:19`: `_f`'s TypeVar becomes a PEP 695 type parameter
   - UP041 `src/spur/pool.py:184`: catch the builtin `TimeoutError`
   - UP017 `src/spur/records.py:67`: `datetime.UTC`

   Five comments then state a reason that D-09 makes false, and CODING_VALUES says a
   comment carries the constraint that forced the choice: `build_errors.py`'s docstring,
   `pool.py`'s except comment, `records.py`'s handler-base comment and `_parse_level`
   docstring, and `tests/test_pool.py`'s `_processes` docstring. Planning found them
   with `git grep -n -e '3\.10' -e '3\.11' -- src tests`.

2. **`records._JsonHandler`'s type-checking-only base class goes.** Its whole comment is
   the argument for a 3.10 CI leg that D-09 removes. Keeping the indirection would need
   a comment explaining a constraint that no longer exists. `_parse_level` is different:
   `logging.getLevelName` is a legitimate API, not a workaround, so only its docstring's
   stated reason changes and its code stays. L23 lists both construct changes and the
   three ruff-forced ones as the unwind list for a future widening. That is D-09's
   "costly" reversibility, made concrete.

3. **The one-entry matrix is kept** (Claude's discretion in CONTEXT). The job stays
   `test (3.12)`, the name `required-jobs.txt` and pr.land use, and widening stays a
   one-line change -- plus, per L23, a real interpreter leg for the new floor.

4. **Changing `pyproject.toml` triggers a reinstall.** It is a prerequisite of
   `.venv/.installed`, so the next `make` target re-runs `pip install -e '.[dev]'`
   (about a minute of pip output) with the interpreter already in the venv (3.12.13).
   That is expected, not a failure.

5. **The documents are Plan 05-05's job.** README, AGENTS.md, CODING_VALUES.md,
   HOW_TO_DEVELOP section 0, the architecture docs and `.planning/` still say
   "3.10-3.12" after this plan; 05-05 corrects them, together with the stale CI claims
   (D-07).

6. **The ruleset is not re-run here.** Plan 05-03 applied it with the post-D-09 set
   (`test (3.12)`, `vendor-bundle`, `image`), so dropping the 3.10 line from
   `required-jobs.txt` brings the list into line with the wall instead of away from it.
   Task 3 only reads the ruleset back and asserts the two are equal (D-12). That read is
   live and read-only.

7. **Ten files, at the checker's warning line, accepted.** Every edit is small and
   surgical -- a version string, a ruff-forced one-liner, or a comment made true -- and no
   task touches more than five files (Task 1: one; Task 2: four; Task 3: five). A split
   would add a wave and a second hand-off, with the matrix and the required list still
   needing to change in one commit.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: `L23` appended -- Python 3.12 only (supersedes L01's floor) (D-08, D-09)</name>
  <reversibility rating="one-way">`docs/architecture/decision_log.md` is append-only by policy -- a wrong entry is superseded, never edited. The human decided to append L23 (D-08), so there is no checkpoint.</reversibility>
  <files>docs/architecture/decision_log.md</files>
  <read_first>
    - `docs/architecture/decision_log.md`: the header (lines 1-10); `L01` (lines 11-19), whose floor is superseded and whose text stays byte-identical; `L14` (lines 128-143), which described the floor as "checked by execution"; `L17`/`L18`/`L21` for the "(supersedes …)" heading and the sentence naming what is superseded; `L22` (the last entry) for the format
    - `.planning/phases/05-ci-observed-green/05-CONTEXT.md` D-08, D-09 (why, the rejected alternatives, reversibility), `<specifics>` (why mypy did not see the 3.10 break) and "Wording the user wants kept"
    - `.planning/phases/05-ci-observed-green/05-RESEARCH.md` "Pitfall 4"
    - This plan's flagged assumptions 1-3
  </read_first>
  <action>
Append, after `L22`, `## L23 — Python 3.12 only (supersedes L01's floor)` with
`Date: 2026-09-25.` on its own line. Per D-08: append only; L01 is superseded by this
heading and body, never edited.

Content, in the L21/L22 style:
- **Lead.** L01 recorded 3.10-3.12 as "detected, not chosen" (quote those words): the
  floor was never a requirement. The ceiling reasoning stands unchanged: `cadquery-ocp`
  publishes no wheels past 3.12; widen when a wheel *and* a consumer appear.
- **Why the floor goes.** mypy cannot hold a floor below 3.12, because numpy's bundled
  stubs use `type` statements. Ruff's 3.10 target held syntax only. So the floor was checked
  by CI's 3.10 run alone, and that let `logging.StreamHandler[TextIO]` -- runtime
  subscripting needs 3.11 -- reach `main` unimportable on 3.10 (runs 35993984796,
  36028253714, 36028759311; fixed `990d1fe`). The Docker image, the dev machine and the
  pre-commit hook are all 3.12. With one version, the type checker's version is the
  runtime and the gap closes structurally.
- **What changes.** `requires-python = ">=3.12,<3.13"` (the upper bound makes L01's
  ceiling checkable: pip refuses instead of building OpenCascade for minutes). Ruff
  `target-version = "py312"`; mypy `python_version = "3.12"` unchanged. CI matrix
  `["3.12"]`, keeping the job name `test (3.12)`. `make venv` picks `python3.12` only.
  `pr.land`'s required list loses the 3.10 job in the same commit as the matrix (L22);
  the ruleset on `main` never required it (D-12).
- **Who is affected.** `pip install` on 3.10 or 3.11 is now refused by pip. Ubuntu 22.04
  users use Docker, which needs no local Python, or `uv`.
- **What 3.12-only adopts: the unwind list.** At planning, `ruff check --target-version
  py312` reported three findings: PEP 695 type parameters on `params._f` (UP047), the
  builtin `TimeoutError` in `pool.py` (UP041), `datetime.UTC` in `records.py` (UP017).
  Also `records._JsonHandler` now subclasses `logging.StreamHandler[TextIO]` directly.
- **Rejected.** 3.11-3.12, which keeps the gap. 3.10 in the pre-commit hook, which puts
  a 3.10 on every clone and roughly doubles the hook's time.
- **Reversibility: costly.** Widening back is a one-line matrix change plus a new
  entry, but every construct in the unwind list must be undone, and the published
  `requires-python` refuses installs that worked before.
- **Reason:** the most likely future regression is lowering the floor for one machine
  without a real interpreter at the new floor in CI. Any widening must bring that
  interpreter leg in the same change, because mypy cannot check a floor below its own
  `python_version` (RESEARCH Pitfall 4).

**Commit** with plain `git commit`, for example
`docs(05-04): L23 -- Python 3.12 only, superseding L01's floor`.
  </action>
  <verify>
    <automated>grep -nE "^## L23 .*supersedes L01" docs/architecture/decision_log.md</automated>
    <fails_when>exit 1 -- no L23 heading naming the superseded L01</fails_when>
    <automated>(for s in 'detected, not chosen' 990d1fe 35993984796 36028253714 36028759311 UP047 UP041 UP017 '>=3.12,<3.13' 'Date: 2026-09-25'; do awk '/^## L23 /,0' docs/architecture/decision_log.md | grep -q -- "$s" || { echo "L23 lacks: $s"; exit 1; }; done)</automated>
    <fails_when>non-zero exit, with a `L23 lacks:` line naming the missing evidence</fails_when>
    <automated>C=$(git log -1 --format=%H -- docs/architecture/decision_log.md) && D=$(git show --format= "$C" -- docs/architecture/decision_log.md) && test -z "$(printf '%s\n' "$D" | grep -E '^-[^-]')"</automated>
    <fails_when>non-zero exit -- the L23 commit removed or changed a line of the log (L01 edited in place), or the commit could not be read</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/decision_log.md` ends with `## L23 — Python 3.12 only (supersedes L01's floor)`, after L22, dated 2026-09-25
    - L23 quotes "detected, not chosen", keeps the ceiling reasoning, cites the three runs and `990d1fe`, lists the unwind list, the rejected alternatives and the reversibility
    - The L23 commit removes no line from the log; L01 is byte-identical
    - `git show --stat --format= HEAD` lists exactly `docs/architecture/decision_log.md`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The 3.12-only decision is on file with its trigger, what it adopts, and what a widening would have to undo. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: The package targets Python 3.12 -- `pyproject.toml`, and what ruff now requires (D-09)</name>
  <reversibility rating="costly">Widening back means unwinding PEP 695 syntax, `datetime.UTC`, the builtin TimeoutError catch and the direct generic base, and re-publishing `requires-python`. Decided by the human (D-09); flagged, not gated.</reversibility>
  <files>pyproject.toml, src/spur/params.py, src/spur/pool.py, src/spur/records.py</files>
  <read_first>
    - `pyproject.toml` lines 5-12 (`requires-python`), 35-66 (`[tool.ruff]`, the `UP` rule's comment) and 80-95 (`[tool.mypy]`: the comment above `python_version`, and the `disallow_any_explicit` comment with its directory list)
    - `src/spur/params.py` lines 1-30 (`T = TypeVar("T")`, `_f`)
    - `src/spur/pool.py` lines 165-215 (the `except asyncio.TimeoutError:` block and its long comment -- only its first paragraph changes)
    - `src/spur/records.py` lines 28-40 (imports), 55-70 (`_JsonFormatter.format`), 105-150 (the handler base and `_parse_level`)
    - `tests/test_records.py`: the tests that exercise `_JsonHandler` and `configure()`, which must keep passing unchanged
    - `docs/architecture/decision_log.md` L23 (Task 1)
    - This plan's flagged assumptions 1, 2 and 4
  </read_first>
  <action>
**`pyproject.toml`** (per D-09):
- `requires-python = ">=3.12,<3.13"`.
- `[tool.ruff] target-version = "py312"`. Its trailing comment becomes: one supported
  interpreter (L23); `cadquery-ocp` has no wheels past 3.12. The claim about CI running
  two versions goes.
- The `[tool.mypy]` comment above `python_version`: keep the numpy-stub reason (numpy's
  bundled stubs use `type` statements, and mypy refuses to read them below 3.12). Replace
  the second half, the part about ruff holding the language floor and CI's real 3.10
  run, with: 3.12 is also the only runtime (L23), so the type checker's version is the
  runtime's. `python_version = "3.12"` itself is unchanged.
- In the `disallow_any_explicit` comment, the parenthetical list of what mypy checks
  gains `scripts` (added to `make typecheck` in Plan 05-01).

**Then run `.venv/bin/ruff check .`** (after `make lint` has reinstalled the venv, or
directly) and fix every finding it reports -- the three below, and anything in
`scripts/`:
- `src/spur/params.py`: declare `_f` with a PEP 695 type parameter; remove the
  module-level `TypeVar` and its import.
- `src/spur/pool.py`: catch the builtin `TimeoutError`. Replace only the first
  paragraph of the comment beneath it (the explanation of why the qualified asyncio
  name was used): since 3.11 the asyncio name is an alias of the builtin, 3.12 is the
  floor (L23), and `asyncio.wait_for` raises exactly this class. The paragraphs about
  `Executor.shutdown` and `_processes` stay word for word.
- `src/spur/records.py`: `datetime.UTC` in `_JsonFormatter.format`.

**`src/spur/records.py`, the rest of D-09 in this file:**
- Remove the `if TYPE_CHECKING:` / `else:` block above `_JsonHandler`, which gives the
  type checker and the interpreter different base classes. `_JsonHandler` subclasses
  `logging.StreamHandler[TextIO]` directly. In place of the old comment, two or three
  lines: subscripting `StreamHandler` at runtime needs 3.11+; the floor is 3.12 (L23);
  this exact construct broke the old 3.10 CI leg (runs 35993984796, 36028253714), so any
  widening below 3.11 must unwind it. Drop `TYPE_CHECKING` from the `typing` import if
  nothing else uses it.
- `_parse_level`'s docstring: the parenthetical ruling out
  `logging.getLevelNamesMapping()` states a reason D-09 retires. Give a true one: it is
  3.11+, and `getLevelName` already answers the same question. The function's code is
  unchanged.

**Commit** with plain `git commit`, for example
`build(05-04): target Python 3.12 only; adopt what ruff's UP rules now require`.
  </action>
  <verify>
    <automated>grep -q 'requires-python = ">=3.12,<3.13"' pyproject.toml && grep -q 'target-version = "py312"' pyproject.toml && grep -q 'python_version = "3.12"' pyproject.toml</automated>
    <fails_when>non-zero exit -- a version setting is missing or wrong</fails_when>
    <automated>.venv/bin/ruff check .</automated>
    <fails_when>non-zero exit, or any finding listed</fails_when>
    <automated>grep -n -e '_StreamHandlerBase' -e 'floor is 3\.10' -e 'floor CI also runs' src/spur/records.py src/spur/pool.py; test $? -eq 1</automated>
    <fails_when>non-zero exit -- the type-checking-only base or a comment reasoning from the 3.10 floor survived (lines printed), or grep itself failed (status 2)</fails_when>
    <automated>make test PYTEST_ARGS="tests/test_records.py tests/test_pool.py tests/test_calc.py tests/test_api.py -q"</automated>
    <fails_when>non-zero exit, or a summary line reporting `failed` or `error`</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` has `requires-python = ">=3.12,<3.13"`, `target-version = "py312"`, `python_version = "3.12"`; the mypy comment keeps the numpy-stub reason and cites L23; the `disallow_any_explicit` comment's list includes `scripts`
    - `src/spur/params.py` declares `_f` with a type parameter list and has no `TypeVar` import
    - `src/spur/pool.py` has `except TimeoutError:`; its `Executor.shutdown` and `_processes` paragraphs are unchanged
    - `src/spur/records.py` uses `datetime.UTC`, and `_JsonHandler`'s base is `logging.StreamHandler[TextIO]` in the class statement itself
    - `.venv/bin/ruff check .` reports nothing
    - `05-04-SUMMARY.md` records the ruff findings actually fixed (expected: the three from planning, plus any in `scripts/`)
    - `git show --stat --format= HEAD` lists exactly the four files in `<files>`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The package declares and is checked against 3.12 only, and the code uses what that target requires, with every workaround for the old floor gone or explained. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: The build and CI run Python 3.12 only -- `make venv`, the matrix, the required list, and the comments that argued from two versions; the ruleset read back equal to the list (D-05, D-09, D-12)</name>
  <precondition>`gh auth status` exits 0 (the live, read-only read-back of the rules on `main`).</precondition>
  <files>Makefile, .github/workflows/ci.yml, .github/workflows/required-jobs.txt, src/spur/build_errors.py, tests/test_pool.py</files>
  <read_first>
    - `Makefile` lines 8-12 (the `PYTHON ?=` loop and its comment) and 31-40 (`$(STAMP)`'s refusal text)
    - `.github/workflows/ci.yml` (whole file): the matrix and the comment Plan 05-02 added
    - `.github/workflows/required-jobs.txt` (whole file)
    - `.planning/phases/05-ci-observed-green/05-03-SUMMARY.md`: the ruleset as Plan 05-03 read it back (the three required checks this task's list must equal)
    - `tests/test_pr_land.py`: the drift test that must stay green
    - `src/spur/build_errors.py` (whole file): the docstring paragraph on why `BuildTimeout` is its own class
    - `src/spur/app.py` around `except BuildTimeout` (about line 419) and `src/spur/pool.py` where `BuildTimeout` is raised -- what the class is actually used for, so the rewritten docstring states only what is true
    - `tests/test_pool.py` `test_executor_processes_attribute_still_exists`'s docstring
  </read_first>
  <action>
**`Makefile`** (per D-09):
- The interpreter loop tries `python3.12` alone. Its comment: `cadquery-ocp` publishes
  wheels up to CPython 3.12 and spur supports 3.12 only (L23). Keep the reason for
  naming the interpreter at all: a bare `python3` sends pip off to build OpenCascade.
- `$(STAMP)`'s first refusal line becomes `make: no python3.12 on PATH.`. The second
  says `cadquery-ocp` has no wheels past 3.12 and spur supports 3.12 only (L23). The
  third (brew, or `make test-image`) stays. Keep the refuse-explain-exit shape.

**`.github/workflows/ci.yml`**: `python: ["3.12"]`. It stays a one-entry matrix, so the
job is still reported as `test (3.12)`.

**`.github/workflows/required-jobs.txt`**: delete the 3.10 test job's line in this same
commit, as the file's header and L22 require. Three jobs remain -- the three the ruleset
on `main` already requires (D-12, flagged assumption 6). After the commit, run the
read-back in `<verify>` and paste its output into `05-04-SUMMARY.md`; the ruleset is not
changed here.

**`src/spur/build_errors.py`**: rewrite the docstring paragraph on why `BuildTimeout`
is not a builtin. Its stated reason -- two distinct timeout classes on the old floor,
with CI running two versions -- is gone. Write what is true: it was introduced for that
reason, which L23 retired, and it is kept because it names the one timeout `app.py`
turns into a 503 with its own error `type`, distinct from any other timeout. Confirm
that against the code you read first. Keep the class line and its `noqa` comment.

**`tests/test_pool.py`**: in `test_executor_processes_attribute_still_exists`'s
docstring, replace the clause about CI also running another version with: checked on
CPython 3.12.13, the only supported interpreter (L23).

Wherever a rewritten comment or docstring (here or in Task 2) mentions the old floor as
history, call it "the retired 3.10 floor (L23)" or name the failed `test (3.10)` runs. Do
not write the phrase "Python" followed by "3.10": Plan 05-05's phase-wide residue check
treats that phrase outside the decision log as a support claim.

**Commit** with plain `git commit`, for example
`ci(05-04): run the gate on Python 3.12 only; make venv picks python3.12`.
  </action>
  <verify>
    <automated>grep -q 'python: \["3.12"\]' .github/workflows/ci.yml && grep -q 'for p in python3.12; do' Makefile</automated>
    <fails_when>non-zero exit -- the matrix or the interpreter loop was not narrowed</fails_when>
    <automated>test "$(grep -v '^#' .github/workflows/required-jobs.txt | grep . | sort | tr '\n' ';')" = 'image;test (3.12);vendor-bundle;'</automated>
    <fails_when>non-zero exit -- the required list is not exactly `image`, `test (3.12)`, `vendor-bundle`</fails_when>
    <automated>R=$(gh api repos/halfb00t/spur/rules/branches/main --jq '[.[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context] | sort | join(";")') && L=$(grep -v '^#' .github/workflows/required-jobs.txt | grep . | LC_ALL=C sort | paste -sd ';' -) && echo "ruleset: $R" && echo "list:    $L" && test -n "$R" && test "$R" = "$L"</automated>
    <fails_when>non-zero exit -- the live ruleset's required checks differ from `required-jobs.txt` (both printed above), the ruleset has none, or `gh` failed</fails_when>
    <automated>make test PYTEST_ARGS="tests/test_pr_land.py tests/test_pool.py -q"</automated>
    <fails_when>non-zero exit, or a summary line reporting `failed` or `error` -- in particular the drift test, if the list and the matrix disagree</fails_when>
    <automated>OUT=$(make VENV="$(mktemp -d)/v" PYTHON= venv 2>&1); RC=$?; printf '%s\n' "$OUT"; test "$RC" -ne 0 && printf '%s\n' "$OUT" | grep -q 'no python3.12 on PATH'</automated>
    <fails_when>non-zero exit -- with no interpreter, `make venv` did not refuse, or refused without the new message</fails_when>
    <automated>git grep -n -e 'python3\.1[01]' -e 'py310' -e '3\.10 and 3\.12' -e 'CI also runs 3\.10' -e 'CI runs both' -- Makefile pyproject.toml .github src tests docker bench scripts; test $? -eq 1</automated>
    <fails_when>non-zero exit -- a line still names another interpreter or claims CI runs two versions (the lines are printed above), or `git grep` itself failed (status 128)</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `Makefile` tries `python3.12` only and refuses with `make: no python3.12 on PATH.`
    - `.github/workflows/ci.yml` has `python: ["3.12"]` and is otherwise unchanged
    - `.github/workflows/required-jobs.txt` lists exactly `test (3.12)`, `vendor-bundle`, `image`, and changed in the same commit as the matrix
    - The ruleset on `main`, read back live, requires exactly the same three checks; `05-04-SUMMARY.md` holds that read-back
    - `src/spur/build_errors.py`'s docstring and `tests/test_pool.py`'s docstring no longer claim CI runs a second version
    - `git show --stat --format= HEAD` lists exactly the five files in `<files>`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The build, CI and the merge gate agree on one interpreter, and no code comment argues from a floor that no longer exists. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` → pip on a user's machine | `requires-python` decides whether an install is attempted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-15 | Tampering | The venv reinstall `pyproject.toml`'s change triggers | low | accept | `pip install -e '.[dev]'` without `-U` keeps already-satisfied versions; no dependency is added or changed. No package-manager install of a new package happens in this plan. |
| T-05-16 | Denial of Service | Users on Python 3.10/3.11 | low | accept | D-09: pip now refuses with a clear message instead of failing minutes into an OpenCascade build; Docker (no local Python) or `uv` is the path, documented in 05-05. |
| T-05-17 | Repudiation | `docs/architecture/decision_log.md` | low | mitigate | Append-only, checked by the diff check in Task 1. |
</threat_model>

<verification>
- L23 appended, L01 untouched
- `pyproject.toml`, `Makefile`, `ci.yml`, `required-jobs.txt` agree on 3.12
- `.venv/bin/ruff check .` clean at py312; the targeted residual grep finds no second interpreter
- The ruleset on `main` requires exactly the jobs `required-jobs.txt` lists (live read-back)
- `make verify` exits 0

Feedback latency (accepted on purpose): each task's first automated check -- a
`grep` -- is its fast signal, well under 30 s. The trailing `make verify` (~42 s) stays:
L13 makes it the one gate, and the pre-commit hook already runs it on every commit, so it
adds no wait the commit has not paid.
</verification>

<success_criteria>
ROADMAP SC-3's code and config half, and the L23 half of SC-4: `requires-python`, ruff's
`target-version`, the CI matrix, the Makefile's interpreter choice and the decision-log
entry superseding L01's floor all agree on 3.12, and `make verify` is green. The README
and the other documents follow in Plan 05-05.
</success_criteria>

<output>
Create `.planning/phases/05-ci-observed-green/05-04-SUMMARY.md` when done.
</output>
