---
phase: 06-address-tech-debt-merge-gate-solid-cache
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/skip_tokens.py
  - tests/test_skip_tokens.py
  - tests/test_pr_land.py
  - docs/HOW_TO_DEVELOP.md
  - docs/tech_debt/active/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md
  - docs/tech_debt/resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md
  - docs/tech_debt/INDEX.md
autonomous: true
requirements: [D-01, D-02, D-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-02: the commit-msg hook hands the whole buffer git gives it to `find_skip_tokens` -- no cut, no read of `GIT_EDITOR`: a real editor-session commit (no `-v`) whose message hides a skip token below a hand-typed git cut line is refused and HEAD does not move (scratch-clone probe; before the fix the same probe created the commit and git recorded the token -- planning, 2026-09-25)"
    - "D-02: `tests/test_skip_tokens.py::test_hook_refuses_a_token_below_a_git_cut_line_whatever_git_editor_says` passes for `GIT_EDITOR` set to `vi`, unset, and `:`"
    - "D-03 (hook half): every refusal names each matched token with the 1-based buffer line it starts on (`line N: '<token>'`) and adds one sentence containing `commit without -v` -- a `git commit -v`-shaped buffer whose appended diff names a token is refused with that sentence, proven by a test and by a real `pre-commit run --hook-stage commit-msg`"
    - "D-03 (doc half): `docs/HOW_TO_DEVELOP.md` §6 carries the same one line in Russian (`git commit -v`, `без \\`-v\\``); per 06-RESEARCH.md Pitfall 2 there is no editor-session text in §6/§8 to correct -- the only stale claim, L22's \"above git's `commit -v` scissors line\", is amended by L25 in Plan 06-04, never edited"
    - "`find_skip_tokens` is unchanged in behaviour and remains the one skip-token search for the hook and `make pr.land`; the cut-line regex, `message_to_check`, the editor-only branch and their tests are gone (D-02)"
    - "D-01: `docs/tech_debt/resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md` reads `Status: resolved`; its `Resolved in:` names Task 1's commit (the one that changed `scripts/skip_tokens.py`); it was `git mv`'d, with its INDEX row moved, in Task 2's commit -- the one that adds the §6 line, never a bookkeeping-only follow-up"
    - "Every commit in this plan is a plain `git commit` through the installed hooks, and no commit message quotes a skip token or a line identical to git's cut line"
    - "`make verify` passes (L13)"
  prohibitions:
    - statement: "Never a second skip-token regex or search: the hook and `make pr.land` both call `scripts.skip_tokens.find_skip_tokens`, whose pattern and behaviour do not change (CONTEXT.md code_context, Phase 5 D-02)."
      status: resolved
      verification: judgment
    - statement: "The gate only tightens: no message the hook refused before this plan is accepted after it, and no flag, environment variable or config key is added that makes the hook accept a token it found."
      status: resolved
      verification: judgment
    - statement: "No cut of any kind comes back -- config-driven (`commit.verbose`/`commit.cleanup`) or content-driven -- anywhere in `find_skip_tokens`'s call path (D-02 rejected the config-driven cut)."
      status: resolved
      verification: judgment
    - statement: "`.pre-commit-config.yaml`'s `no-skip-token` wiring and what `make verify` checks are unchanged; no new CI job (CONTEXT.md scope)."
      status: resolved
      verification: judgment
    - statement: "The decision log is not touched by this plan; L22 stays as written (D-10 -- the amendment is L25, Plan 06-04)."
      status: resolved
      verification: judgment
  artifacts:
    - path: "scripts/skip_tokens.py"
      provides: "`main` reading the whole buffer; the refusal naming token and line and the `-v` caveat"
      contains: "commit without -v"
    - path: "tests/test_skip_tokens.py"
      provides: "D-02's regardless-of-GIT_EDITOR refusal, the `-v`-diff refusal with the caveat, both-lines naming"
      contains: "def test_hook_refuses_a_token_below_a_git_cut_line_whatever_git_editor_says("
    - path: "docs/HOW_TO_DEVELOP.md"
      provides: "§6's one-line `-v` caveat, in Russian"
      contains: "git commit -v"
    - path: "docs/tech_debt/resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md"
      provides: "The retired debt record: option 1 taken, the probe before/after"
      contains: "Status: resolved"
  key_links:
    - from: "scripts/skip_tokens.py main()"
      to: "scripts/skip_tokens.py find_skip_tokens()"
      via: "the whole raw buffer, no cut"
      pattern: "find_skip_tokens(raw)"
    - from: ".pre-commit-config.yaml no-skip-token"
      to: "scripts/skip_tokens.py main()"
      via: "`.venv/bin/python -m scripts.skip_tokens <message file>` at the commit-msg stage (unchanged)"
      pattern: "scripts.skip_tokens"
---

<objective>
Retire the `must` item the commit-msg hook left open in 05-06 (D-02, D-03): in an editor
session without `-v`, a cut line typed by hand hides a skip token from the hook while git
records the token. Re-probed at planning in a scratch clone of this branch (2026-09-25):
an editor that writes `safe subject`, a blank line, git's cut line and a token produced a
commit, and `git log -1 --format=%B` showed the token kept.

**D-02 -- drop the cut entirely.** `main()` hands the whole buffer to `find_skip_tokens`;
the cut-line regex, the editor-only branch keyed on `GIT_EDITOR`, `message_to_check`
(CONTEXT.md discretion: deleted -- nothing else calls it) and their tests go. Accepted cost:
a `git commit -v` whose appended staged diff names a token is refused; `git config --get
commit.verbose` reads empty here (2026-09-25).

**D-03 -- say so where it bites.** The refusal names each token and the buffer line it sits
on, and one sentence tells a `-v` author to commit without `-v`; §6 of
`docs/HOW_TO_DEVELOP.md` gets the same line in Russian. 06-RESEARCH.md Pitfall 2: §6/§8 hold
no editor-session explanation to correct -- D-03 is an addition there; L22's "above git's
`commit -v` scissors line" is amended by L25 (Plan 06-04), never edited.

**Task 1, the tracer**: tests flipped RED, the hook changed, and a real editor-session
commit refused in a scratch clone -- end to end through git, pre-commit and the hook.
**Task 2**: §6's line and the debt file retired in the commit that adds it.

Purpose: the hook is a wall, not a heuristic -- no content it is handed is trusted.
Output: `scripts/skip_tokens.py`, its tests, one docstring sentence in
`tests/test_pr_land.py`, §6, the resolved debt file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-address-tech-debt-merge-gate-solid-cache/06-CONTEXT.md
@.planning/phases/06-address-tech-debt-merge-gate-solid-cache/06-RESEARCH.md
@.planning/phases/06-address-tech-debt-merge-gate-solid-cache/06-PATTERNS.md
@CLAUDE.md
@docs/CODING_VALUES.md
@docs/tech_debt/active/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md
</context>

<artifacts_this_phase_produces>
Artifacts this plan produces (new, changed or removed symbols and paths):

- `scripts/skip_tokens.py`:
  - `main(argv: list[str] | None = None) -> int` -- **changed**: `find_skip_tokens(raw)` on
    the whole buffer; the refusal prints `line N: '<token>'` per token and a `-v` sentence
    containing `commit without -v`
  - `_SCISSORS` -- **removed** (the regex and its comment block)
  - `message_to_check(raw: str) -> str` -- **removed**
  - `import os` -- **removed**
  - module docstring's two-callers paragraph and `find_skip_tokens`'s docstring -- rewritten
    (no caller cuts); `find_skip_tokens` and `SKIP_TOKEN` otherwise unchanged
- `tests/test_skip_tokens.py`:
  - new `test_hook_refuses_a_token_below_a_git_cut_line_whatever_git_editor_says`
    (parametrised `git_editor` in `["vi", None, ":"]`)
  - new `test_hook_refuses_a_token_in_the_appended_commit_v_diff_and_says_to_commit_without_v`
  - `test_token_above_and_below_the_scissors_line_is_found_once` -> renamed
    `test_hook_names_a_token_above_and_below_a_cut_line_on_both_lines`
  - **removed**: `test_token_only_below_the_scissors_line_gives_no_tokens`,
    `test_hook_without_an_editor_refuses_a_token_below_a_cut_line`,
    `test_message_to_check_returns_all_of_raw_when_there_is_no_scissors_line`
  - `test_find_skip_tokens_searches_the_whole_text_it_is_given` -- docstring only
- `tests/test_pr_land.py`: `test_message_refusals_checks_the_whole_body_even_below_a_git_cut_line`
  -- docstring only
- `docs/HOW_TO_DEVELOP.md` §6: one new paragraph (Russian)
- `docs/tech_debt/active/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md` ->
  `docs/tech_debt/resolved/…` (`git mv`, `Status: resolved`, `Resolved in:`, Resolution)
- `docs/tech_debt/INDEX.md`: the row moves to `## Resolved`
- `.planning/phases/06-address-tech-debt-merge-gate-solid-cache/06-02-SUMMARY.md`
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **The probe discriminates -- run at planning against the pre-fix hook.** A scratch
   clone of this branch with the working-tree `scripts/skip_tokens.py` committed, `.venv`
   symlinked, `pre-commit install`ed, and `GIT_EDITOR` pointed at a script that copies a
   prepared message (`safe subject`, blank line, the 51-character cut line, `[skip ci]`)
   into the buffer: `SKIP=verify git commit --allow-empty` **succeeded** (hook `Passed`,
   HEAD moved, recorded message kept the token). Task 1's verify runs the same command
   shape and must now see the commit refused and HEAD unmoved. The clone is throwaway; the
   real branch is never used for the probe.
2. **Line numbers are exact without a second pattern.** `find_skip_tokens(raw)` returns
   the matched text in order of appearance, non-overlapping; searching `raw` for each
   returned text starting at the end of the previous one lands on that match's own start
   (an earlier occurrence of the same text would itself be the leftmost match). The line
   is the count of newlines before that position, plus one. This keeps `find_skip_tokens`
   the only search (prohibition 1).
3. **Tests need no `GIT_EDITOR` hygiene any more** -- `main` no longer reads it; the
   D-02 test sets it three ways only to prove that.
4. **Commit messages cannot quote the probe.** The installed hook refuses any message
   carrying a token anywhere, before and after this plan; every commit message describes
   the case in words.
5. **`Resolved in:` names Task 1's commit; the file moves in Task 2's** (Phase 5's
   mechanism: `docs/tech_debt/resolved/2026-09-25-ship-note-skip-token-leaks-into-squash-merge.md`,
   "Resolution"). The moving commit carries §6's line -- the doc half of D-03 -- so it
   completes the fix rather than following it (D-01).
6. **Spec-less probe fallback: ⚠ spec-less probe fallback skipped: phase has no
   requirement IDs to probe (visible skip).** Prohibitions are recalled from CONTEXT.md,
   descriptor-less.
</flagged_assumptions>

<prohibition_recall>
Recalled from CONTEXT.md (no SPEC `## Prohibitions`): "never a second skip-token regex"
(kept, 1); the gate only tightens, no bypass knob (kept, 2 -- Phase 5's standing
prohibition); no config-driven or content cut returns (kept, 3, D-02's rejected option);
no change to the hook wiring, to `make verify` or to CI (kept, 4, CONTEXT.md scope); the log
is append-only and not touched here (kept, 5, D-10). Dropped as routine: "the refusal
must not echo the whole message" (it prints tokens and line numbers only -- owned by
tests). No-silent-drop: 5 kept, 5 authored under `must_haves.prohibitions`, descriptor-less.
</prohibition_recall>

<source_audit>
Phase-wide audit: see `06-01-PLAN.md` `<source_audit>` (identical table). This plan
covers D-01 (this debt file), D-02, D-03 and RESEARCH Pitfall 2.
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End to end -- an editor-session commit that hides a skip token below a hand-typed cut line is refused at `git commit`, and every refusal names the line and the `-v` way out (D-02, D-03)</name>
  <precondition>The commit-msg hook is installed on this clone: `grep -q -- '--hook-type=commit-msg' "$(git rev-parse --git-path hooks)/commit-msg"` exits 0 (Plan 05-01 installed it).</precondition>
  <reversibility rating="reversible">The cut is about twenty lines and its tests are in history (`3e68e74`); nothing outside this repository reads the hook (CONTEXT.md D-02).</reversibility>
  <files>scripts/skip_tokens.py, tests/test_skip_tokens.py, tests/test_pr_land.py</files>
  <read_first>
    - `scripts/skip_tokens.py` (whole file, 121 lines): module docstring (1-28, the two-callers paragraph at 15-22), imports (30-35), `SKIP_TOKEN` (37-45), the cut-line regex and its comment (47-59), `message_to_check` (62-65), `find_skip_tokens` (68-75), `main` (78-117)
    - `tests/test_skip_tokens.py` (whole file, 229 lines): the import on line 20, the cut-line tests at 101-181, `_run_hook` and the subprocess tests at 188-229
    - `tests/test_pr_land.py` lines 431-437 (`test_message_refusals_checks_the_whole_body_even_below_a_git_cut_line` -- its docstring names the hook's cut)
    - `docs/tech_debt/active/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md` (the per-mode probe table; option 1 is D-02)
    - `.pre-commit-config.yaml` (the `no-skip-token` hook: unchanged wiring)
    - `06-CONTEXT.md` D-02, D-03; `06-PATTERNS.md` "scripts/skip_tokens.py::main" and "tests/test_skip_tokens.py"
    - This plan's flagged assumptions 1-4
  </read_first>
  <behavior>
    - For the message `safe subject`, blank line, git's cut line (`# ` + 24 dashes + ` >8 ` + 24 dashes), `[skip ci]`: `main([path])` returns 1 with `GIT_EDITOR` set to `vi`, unset, or `:`; stdout contains `line 4: '[skip ci]'`
    - For a `git commit -v`-shaped buffer (subject, blank line, cut line, git's two `#` instruction lines, `diff --git a/f b/f`, `+a line naming [skip ci] inside the diff`): `main` returns 1; stdout contains `line 7: '[skip ci]'` and `commit without -v`
    - For a token on line 1 and again on line 4 below a cut line: `main` returns 1 and stdout names `'[skip ci]'` twice, once with `line 1` and once with `line 4`
    - A clean message still returns 0; the six token forms and near-misses (`find_skip_tokens` tests) are unchanged; the subprocess and invalid-UTF-8 tests still pass
  </behavior>
  <action>
**RED first (tests/test_skip_tokens.py).** Change the tests below, run
`make test PYTEST_ARGS="tests/test_skip_tokens.py -q"`, and confirm the new and renamed
tests fail on the unchanged hook (it cuts in an editor session and prints no line numbers).
Paste the failure lines into `06-02-SUMMARY.md`; RED and GREEN land in one commit (the
pre-commit hook runs `make verify` with no bypass).

- Import only `find_skip_tokens` and `main` from `scripts.skip_tokens`.
- Delete `test_token_only_below_the_scissors_line_gives_no_tokens`,
  `test_hook_without_an_editor_refuses_a_token_below_a_cut_line` and the `message_to_check`
  test (D-02: the cut's tests go with it).
- New `test_hook_refuses_a_token_below_a_git_cut_line_whatever_git_editor_says`,
  parametrised `git_editor` over `["vi", None, ":"]` (`monkeypatch.delenv` for `None`): the
  hand-typed shape above written to `tmp_path / "COMMIT_EDITMSG"`; assert `main` returns 1
  and `capsys` stdout contains `line 4: '[skip ci]'`. Docstring: D-02 -- a cut line typed by
  hand is byte-identical to git's own, and `GIT_EDITOR` says whether an editor ran, not
  whether `-v` was given, so the hook trusts no cut line (the debt record's probe table).
- New `test_hook_refuses_a_token_in_the_appended_commit_v_diff_and_says_to_commit_without_v`
  (`GIT_EDITOR=vi`): the `-v`-shaped buffer above; assert 1, `line 7: '[skip ci]'` and
  `commit without -v` in stdout. Docstring: D-02's accepted cost and D-03's way out.
- Rename `test_token_above_and_below_the_scissors_line_is_found_once` to
  `test_hook_names_a_token_above_and_below_a_cut_line_on_both_lines`; same message; assert
  1, `'[skip ci]'` appears exactly twice in stdout, and both `line 1: '[skip ci]'` and
  `line 4: '[skip ci]'` appear.
- `test_find_skip_tokens_searches_the_whole_text_it_is_given`: docstring now says neither
  caller cuts (D-02) -- the hook hands in the whole buffer, `pr.land` the whole PR text.
- `test_hook_entry_exits_1_with_the_token_named_for_a_token_message`: also assert
  `line 1` in stdout.

**GREEN (scripts/skip_tokens.py).**
- Delete the `_SCISSORS` regex with its comment block, the `message_to_check` helper, and
  the `os` import; `main` no longer reads the environment.
- `main`: after reading the file as now (keep the `errors="replace"` comment), the verdict
  is `find_skip_tokens` on the whole `raw`. Replace the old cut comment with a why-comment:
  the hook reads everything git hands it because a hand-typed cut line is byte-identical to
  git's own and git tells a hook whether an editor ran, not whether `-v` was given (the probe
  table in `docs/tech_debt/resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md`);
  the one false positive is a `-v` diff that names a token, and the refusal says so (D-02,
  D-03).
- Refusal: keep the header line; then, per token, `      line N: '<token>'` where N is
  the 1-based buffer line the match starts on, found as flagged assumption 2 describes (walk
  `raw` with `str.find` from the previous match's end; newlines before it, plus one); keep
  the two explanation lines; add one line containing exactly `commit without -v`, e.g. that
  if a named line is in the staged diff `git commit -v` appends below the cut line, commit
  without -v -- the hook reads the whole message (D-02); keep the `See docs/tech_debt/...`
  line.
- `find_skip_tokens` docstring: drop the sentences about the hook's cut; say it searches the
  whole text it is given and that no caller cuts (D-02). Code unchanged.
- Module docstring, the two-callers paragraph: both callers hand `find_skip_tokens` whole
  text -- the hook the buffer git hands a commit-msg hook, `make pr.land` the PR title and
  body -- one pattern, so the two checks cannot drift apart; name D-02's cost (a `-v` diff
  naming a token is refused; `commit.verbose` read empty here on 2026-09-25). Leave the
  first, second and over-match paragraphs as they are.

**tests/test_pr_land.py, one docstring.** In
`test_message_refusals_checks_the_whole_body_even_below_a_git_cut_line`, the clause saying
the commit-msg hook's cut has no meaning for PR text becomes: git's `commit -v` cut line
means nothing for this text (the hook no longer cuts either, D-02). No other change.

**Verify, then commit** with plain `git commit`, for example
`fix(06-02): the commit-msg hook reads the whole message, cut line or not` -- describe the
case in words; never quote a token or the cut line in the message (flagged assumption 4).
Record the scratch-clone probe's output in `06-02-SUMMARY.md`.
  </action>
  <verify>
    <automated>make test PYTEST_ARGS="tests/test_skip_tokens.py -q -k 'cut_line or commit_v'"</automated>
    <fails_when>non-zero exit, or the summary line does not begin `5 passed` (three parametrised D-02 cases, the `-v` case, the both-lines case)</fails_when>
    <automated>make test PYTEST_ARGS="tests/test_skip_tokens.py tests/test_pr_land.py -q"</automated>
    <fails_when>non-zero exit, or a summary line reporting `failed`, `error` or `no tests ran`</fails_when>
    <automated>S=$(mktemp -d) && git clone -q . "$S/p" && cp scripts/skip_tokens.py "$S/p/scripts/skip_tokens.py" && ln -s "$PWD/.venv" "$S/p/.venv" && M="$S/msg" && E="$S/editor.sh" && printf 'safe subject\n\n# ------------------------ >8 ------------------------\n[skip ci]\n' > "$M" && printf '#!/bin/sh\ncp "%s" "$1"\n' "$M" > "$E" && chmod +x "$E" && cd "$S/p" && git -c core.hooksPath=/dev/null commit -q --allow-empty -am 'probe: the working-tree hook' && .venv/bin/pre-commit install >/dev/null && H=$(git rev-parse HEAD) && ! SKIP=verify GIT_EDITOR="$E" git commit --allow-empty && test "$(git rev-parse HEAD)" = "$H"</automated>
    <fails_when>non-zero exit -- the editor-session commit was created (HEAD moved) or the probe could not be set up; before the fix this exact command exited 1 with `reject GitHub Actions skip tokens ... Passed`. Run from the repository root; it only touches a throwaway clone under a `mktemp -d` directory</fails_when>
    <automated>F=$(mktemp) && printf 'docs: a subject\n\n# ------------------------ >8 ------------------------\n# Do not modify or remove the line above.\ndiff --git a/f b/f\n+a line naming [skip ci]\n' > "$F" && OUT=$(GIT_EDITOR=vi .venv/bin/pre-commit run --hook-stage commit-msg --commit-msg-filename "$F" --all-files 2>&1); RC=$?; printf '%s\n' "$OUT"; test "$RC" -ne 0 && printf '%s\n' "$OUT" | grep -q "line 6: '\[skip ci\]'" && printf '%s\n' "$OUT" | grep -q 'commit without -v'</automated>
    <fails_when>exit 0 overall with the hook reported `Passed`, or the output lacks `line 6: '[skip ci]'` or `commit without -v`</fails_when>
    <automated>F=$(mktemp) && printf 'docs: a message with nothing to refuse\n' > "$F" && .venv/bin/pre-commit run --hook-stage commit-msg --commit-msg-filename "$F" --all-files</automated>
    <fails_when>non-zero exit -- a clean message is refused</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def message_to_check' scripts/skip_tokens.py` prints `0`; `grep -c '_SCISSORS =' scripts/skip_tokens.py` prints `0`; `grep -c 'os.environ' scripts/skip_tokens.py` prints `0`
    - `grep -n 'find_skip_tokens(raw)' scripts/skip_tokens.py` prints one line inside `main`; `grep -c 'commit without -v' scripts/skip_tokens.py` prints `1`
    - With `C=$(git log -1 --format=%H -- tests/test_skip_tokens.py)` (this task's commit -- Task 2 does not touch the tests): `git diff "$C^" "$C" -- scripts/skip_tokens.py` leaves the `SKIP_TOKEN` pattern lines and the `return [m.group(0) for m in SKIP_TOKEN.finditer(message)]` line unchanged, and `git show --stat --format= "$C"` lists exactly `scripts/skip_tokens.py`, `tests/test_pr_land.py`, `tests/test_skip_tokens.py`
    - `tests/test_skip_tokens.py` contains the three new/renamed test names from `<artifacts_this_phase_produces>` and none of the three removed ones; `grep -c 'message_to_check(' tests/test_skip_tokens.py` prints `0`
    - The scratch-clone probe exits 0 (commit refused, HEAD unmoved)
    - `06-02-SUMMARY.md` records the RED failure lines and the probe output
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The hook refuses a token anywhere in the buffer git hands it, in any commit mode, names the line, and tells a `-v` author the way out; one search still serves both checks. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: §6 carries the `-v` caveat in Russian, and the hand-typed-cut-line debt is retired in that commit (D-03, D-01)</name>
  <precondition>Task 1's commit exists: `git show --name-only --format= "$(git log -1 --format=%H -- scripts/skip_tokens.py)"` lists `tests/test_skip_tokens.py`.</precondition>
  <reversibility rating="reversible">One documentation paragraph and a moved debt file.</reversibility>
  <files>docs/HOW_TO_DEVELOP.md, docs/tech_debt/active/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md, docs/tech_debt/resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md, docs/tech_debt/INDEX.md</files>
  <read_first>
    - `docs/HOW_TO_DEVELOP.md` lines 15-30 (§0, the `L22` bullet -- still true, untouched) and 109-124 (§6: the ship-note paragraph the new line follows); the file is Russian -- match its register
    - `docs/tech_debt/active/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md` (whole file)
    - `docs/tech_debt/resolved/2026-09-25-ship-note-skip-token-leaks-into-squash-merge.md` (resolved-file shape)
    - `docs/tech_debt/INDEX.md` (whole file)
    - `06-RESEARCH.md` Pitfall 2 (nothing to correct in §6/§8); `06-02-SUMMARY.md` as Task 1 left it
  </read_first>
  <action>
**Sha.** `S=$(git log -1 --format=%h --abbrev=7 -- scripts/skip_tokens.py)` is Task 1's
commit.

**§6 (D-03, Russian, one paragraph after the ship-note paragraph that ends with
«невидима этому репозиторию.»).** Same content as the hook's sentence: the hook reads the
whole message, including the staged diff `git commit -v` appends below the cut line; if
that diff names a skip token, the commit is refused -- commit without `-v` or reword. It
must contain the literal `git commit -v` and the literal `без` followed by `` `-v` ``. A
fitting wording: «Хук читает сообщение целиком, вместе с diff, который `git commit -v`
дописывает под линией отреза: если этот diff называет токен пропуска CI, коммит будет
отказан — коммить без `-v` или перефразируй.» Change nothing else in the file (Pitfall 2:
there is no editor-session text to correct; the §0 `L22` bullet stays true).

**Retire the debt file (D-01).** `git mv` it from `docs/tech_debt/active/` to
`docs/tech_debt/resolved/`. In it: `Status: resolved`; `Resolved in: <S>` right after
`Date:`; append `## Resolution (2026-09-25)`: `Resolved in: <S>` is Plan 06-02 Task 1's
commit (its subject) -- not the present commit, which adds §6's `-v` line (D-03's doc half)
and moves this file, completing the fix (a file cannot carry its own commit's sha). Then:
option 1 of "Next step" was taken (drop the cut entirely, D-02) and option 2 rejected (a
`-v` typed on the command line stays invisible to config); what now exists (`main` searches
the whole buffer; the refusal names token, line and the `-v` way out; the tests by name);
the evidence -- the scratch-clone probe before (commit created, token recorded; planning,
2026-09-25) and after (refused, HEAD unmoved), from `06-02-SUMMARY.md`; the accepted cost
and `commit.verbose` read empty on 2026-09-25. Context and "Why it matters" stay as written.
Remove the trailing `<!-- On resolve ... -->` comment (the resolved records carry none).

**INDEX.** Delete the `## Active` row; append a `## Resolved` row of the existing shape
linking `resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md`, then
`` `<S>` — see the file's own `Resolved in:` field ``.

**Commit** with plain `git commit`, for example
`docs(06-02): document the commit -v caveat; retire the hand-typed cut-line debt`.
  </action>
  <verify>
    <automated>grep -qF 'git commit -v' docs/HOW_TO_DEVELOP.md && grep -qF 'без `-v`' docs/HOW_TO_DEVELOP.md && awk '/^## 6\. /{s=1} /^## 7\. /{s=0} s' docs/HOW_TO_DEVELOP.md | grep -qF 'без `-v`'</automated>
    <fails_when>non-zero exit -- the `-v` line is missing, or it landed outside §6</fails_when>
    <automated>N=2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md; R=docs/tech_debt/resolved/$N; grep -qx 'Status: resolved' "$R" && S=$(sed -n 's/^Resolved in: //p' "$R") && test -n "$S" && FS=$(git show --name-only --format= "$S") && M=$(git log -1 --format=%H -- "$R") && test -n "$M" && FM=$(git show --name-only --no-renames --format= "$M") && printf '%s\n' "$FS" | grep -qx 'scripts/skip_tokens.py' && printf '%s\n' "$FM" | grep -qx "docs/tech_debt/active/$N" && printf '%s\n' "$FM" | grep -qx 'docs/HOW_TO_DEVELOP.md' && printf '%s\n' "$FM" | grep -qx 'docs/tech_debt/INDEX.md' && git merge-base --is-ancestor "$S" "$M^" && test "$(git ls-files -- "docs/tech_debt/active/$N" "$R")" = "$R" && grep -q "](resolved/$N)" docs/tech_debt/INDEX.md && ! grep -q "](active/$N)" docs/tech_debt/INDEX.md</automated>
    <fails_when>non-zero exit -- not resolved, `Resolved in:` is not the hook commit, the move rode in a commit without §6's line or the INDEX row, or an active copy is still tracked</fails_when>
  </verify>
  <acceptance_criteria>
    - With `C=$(git log -1 --format=%H -- docs/tech_debt/resolved/2026-09-25-commit-msg-hook-trusts-a-hand-typed-cut-line.md)` (this task's commit): `git show --stat --format= "$C"` lists `docs/HOW_TO_DEVELOP.md`, `docs/tech_debt/INDEX.md` and the moved debt file only, and `git diff --numstat "$C^" "$C" -- docs/HOW_TO_DEVELOP.md` shows 0 removed lines (§6 gained a paragraph; nothing was rewritten)
    - The resolved file contains `## Resolution (2026-09-25)` and names option 1
    - Both verify commands exit 0
  </acceptance_criteria>
  <done>§6 tells a `-v` author what the hook tells them, and the debt file is resolved, moved and indexed in that commit. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit message → commit-msg hook | Any contributor or tool writes the message; git hands the hook the unprocessed buffer, including `-v`'s appended diff |
| hook verdict → a PR head's checks | A token that passes the hook leaves a PR head with zero recorded checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | `scripts/skip_tokens.py` `main` -- a token hidden below a hand-typed cut line in an editor session | medium | mitigate | Task 1: whole-buffer search, no cut; the scratch-clone editor-session commit is refused and HEAD does not move. |
| T-06-05 | Denial of Service | refusal of a `git commit -v` whose appended diff names a token | low | accept | D-02's accepted cost: `commit.verbose` reads empty here; the refusal names the line and says to commit without `-v`; §6 says the same. |
| T-06-06 | Elevation of Privilege | `git commit --no-verify` skipping the hook | low | accept | Unchanged (T-05-02): the ruleset on `main` refuses a head without green required checks, `make pr.land` refuses a run-less head, and after D-03 (Phase 5) a branch commit's message never becomes `main`'s. |

No package is installed by this plan; no supply-chain row.
</threat_model>

<verification>
- A real editor-session commit hiding a token below a hand-typed cut line is refused in a scratch clone; HEAD does not move (it was created before the fix)
- A `-v`-shaped buffer is refused with `line 6: '[skip ci]'` and `commit without -v` through the real pre-commit commit-msg stage; a clean message passes
- `find_skip_tokens` is unchanged and still the only search; the cut and its tests are gone
- §6 carries the `-v` line; the debt file is resolved and moved in that commit, naming Task 1's sha
- `make verify` exits 0
</verification>

<success_criteria>
No content inside the buffer git hands the commit-msg hook can hide a skip token from it
(D-02); a refusal says which line and what to do about a `-v` diff, in the hook and in §6
(D-03); the debt record is retired in the commit that completes the fix (D-01).
</success_criteria>

<output>
Create `.planning/phases/06-address-tech-debt-merge-gate-solid-cache/06-02-SUMMARY.md` when done.
</output>
