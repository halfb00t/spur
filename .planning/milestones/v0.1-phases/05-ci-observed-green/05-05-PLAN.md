---
phase: 05-ci-observed-green
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - README.md
  - AGENTS.md
  - docs/CODING_VALUES.md
  - docs/HOW_TO_DEVELOP.md
  - docs/architecture/overview.md
  - docs/architecture/packaging.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [REQ-ci-verified]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every document a contributor or agent reads first says Python 3.12 only: `README.md` (the 'Without Docker' line, the `make venv` comment, the CI sentence, the wheels sentence), `AGENTS.md`'s Stack line, `docs/CODING_VALUES.md` (Stack and Project-specific), `docs/HOW_TO_DEVELOP.md` section 0 and `docs/architecture/overview.md`, each citing L23 where it cites L01 (D-09, ROADMAP SC-3)"
    - "`docs/HOW_TO_DEVELOP.md` section 0's list of guarantees names the three new ones, in Russian: the commit-msg hook refusing skip tokens; the ruleset on `main`, which makes GitHub refuse a merge without green `test (3.12)`, `vendor-bundle` and `image` on an up-to-date head, and any direct push (D-12); and `make pr.land` as the path into `main` (L22)"
    - "`docs/architecture/packaging.md` and `.planning/codebase/STACK.md`, `CONVENTIONS.md`, `INTEGRATIONS.md` and `CONCERNS.md` state 3.12 only, and no longer say CI runs two versions or that ruff targets py310"
    - "The stale 'never executed' CI claims are corrected, citing the evidence that exists (D-07): `.planning/codebase/CONCERNS.md`'s CI note and `.planning/codebase/INTEGRATIONS.md`'s CI section cite main push run 35963114939 (`59f02c3`, all four jobs green) and PR #3 head run 36088409707 (`2c4b544`, tree-identical to `bfc9110`), and name the merge gate: the ruleset on `main` (D-12) and `make pr.land` (L22)"
    - "`.planning/REQUIREMENTS.md` REQ-ci-verified reads 'on the supported Python version' (3.12, L23) instead of 'both supported Python versions'; its checkbox and traceability row are left to the phase-completion tooling (D-07, D-09)"
    - "The STATE.md blocker 'CI workflow unverified' is retired: its four-line bullet is gone from Blockers/Concerns, and the existing resolved-items note records it as resolved in Phase 5, citing https://github.com/halfb00t/spur/actions/runs/35963114939 and https://github.com/halfb00t/spur/actions/runs/36088409707 (D-07, ROADMAP SC-4)"
    - "Phase-wide residue check: outside history (the decision log, resolved debt, phase artifacts, generated intel, dated reviews and the ROADMAP's own reframing note), no tracked file claims Python 3.10 or 3.11 support or that CI runs 3.10, none says the workflow never executed, and none says the repository is on a free plan without branch protection (D-12)"
    - "Verification is done against the reframed ROADMAP Phase 5 criteria, applied before planning (D-10), including success criterion 5 added with D-12"
    - "`make verify` passes (L13)"
  prohibitions:
    - statement: "No file records this phase's own squash-commit run URL before `make pr.land` prints it at the merge (D-07). STATE.md cites only the two runs that exist today; the phase's own run is recorded on the next STATE.md touch after the merge. Checked by reading the STATE.md diff."
      status: resolved
      verification: judgment
  artifacts:
    - path: "README.md"
      provides: "Python 3.12 in the install and development sections"
      contains: "Python 3.12"
    - path: "AGENTS.md"
      provides: "The standing brief's stack line, 3.12 only"
      contains: "L23"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "The CI note citing the observed runs"
      contains: "35963114939"
    - path: ".planning/STATE.md"
      provides: "The blocker retired with run URLs"
      contains: "36088409707"
  key_links:
    - from: ".planning/STATE.md"
      to: "https://github.com/halfb00t/spur/actions/runs/35963114939"
      via: "the resolved-blocker note cites the main push run as the evidence"
      pattern: "actions/runs/35963114939"
    - from: "AGENTS.md"
      to: "docs/architecture/decision_log.md"
      via: "the stack line cites L23"
      pattern: "L23"
---

<objective>
Make the record true (D-07, D-09). After Plans 05-01 to 05-04 the code, the build and
the log agree on the new reality, but the prose around them still says what it said on
2026-09-21: "Python 3.10-3.12", "CI runs 3.10 and 3.12", "the workflow has never
executed". An agent reads `AGENTS.md` on every interaction, so a false stack line there
misleads every later session.

**Task 1.** The contributor-facing documents: README, AGENTS.md, CODING_VALUES.md,
HOW_TO_DEVELOP section 0 (which also gains the three new guarantees: the hook, the
ruleset on `main`, `make pr.land`), overview.md.

**Task 2.** The architecture and codebase-map documents: packaging.md and the four
`.planning/codebase/` files, including the "never executed" note, retired with the run
URLs.

**Task 3.** REQ-ci-verified's wording, PROJECT.md's Runtime constraint, and the STATE.md
blocker retired with the two run URLs (D-07). Then the phase-wide residue check. The
verifier works from the reframed ROADMAP criteria (D-10).

Purpose: ROADMAP SC-3's "the README … all agree" and SC-4's blocker retirement, and a
repository where no document contradicts the code.

Output: thirteen files corrected, none structurally changed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-observed-green/05-CONTEXT.md
@.planning/phases/05-ci-observed-green/05-04-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates no code symbols; it edits prose in the thirteen files listed in
`files_modified`, and records the retired blocker in `.planning/STATE.md`'s
Blockers/Concerns section.

The phase's symbols come from `05-01` (`scripts/skip_tokens.py`: `find_skip_tokens`,
`message_to_check`, `main`; the `no-skip-token` hook), `05-02` (`scripts/pr_land.py`:
`land`, `check_head`, `head_refusals`, `required_jobs`, …; `make pr.land`;
`.github/workflows/required-jobs.txt`), `05-03` (`L22`) and `05-04` (`L23`).
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **The document set is wider than CONTEXT's list.** CONTEXT named README, the three
   codebase files, and REQUIREMENTS. Planning ran
   `git grep -n -e '3\.10' -e '3\.11' -e 'py310'` and found the same claim in
   `AGENTS.md` (the standing brief), `docs/CODING_VALUES.md`, `docs/HOW_TO_DEVELOP.md`
   section 0, `docs/architecture/overview.md`, `docs/architecture/packaging.md`,
   `.planning/codebase/CONVENTIONS.md` and `.planning/PROJECT.md`'s Runtime constraint.
   D-09 is "spur supports Python 3.12 only", and a document still saying 3.10 would
   contradict it. Each edit is a line or two.

2. **Left alone on purpose.** History that was true when written stays: the decision
   log's L01 and L14 (append-only), the resolved debt file, `docs/plan-2026-09-21.md`,
   the dated reviews, phase artifacts, the ROADMAP's reframing note, and the generated
   `.planning/intel/` files (regenerated by their own tooling). `PROJECT.md`'s Key
   Decisions table also stays: its L01 row mirrors the log, and rows for L22/L23 are
   added at the phase transition, as L20 and L21 were.

3. **The STATE.md blocker is removed with one scoped edit, not with
   `gsd_run query state.resolve-blocker`.** Planning read that command in
   `~/.claude/gsd-core/bin/lib/state.cjs` (`cmdStateResolveBlocker`). It filters single
   lines beginning with `- `. This blocker is a four-line bullet, so the command would
   delete its first line and leave three orphaned continuation lines inside
   Blockers/Concerns. The edit touches only that section: remove the bullet, extend the
   existing italic resolved-items note.

4. **REQ-ci-verified's checkbox and traceability row are not flipped here.** The
   execute-phase and phase-completion tooling marks requirements complete, as it did
   for REQ-typed-derived-dimensions.

5. **Thirteen files, above the checker's warning line, accepted.** Each edit is a line
   or two of prose -- a version string, a citation, one added sentence -- and no task
   touches more than five files (Tasks 1 and 2: five each; Task 3: three). Splitting
   would add a sixth wave for edits that share one concern: making the record agree with
   what Plans 05-01 to 05-04 built.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: What a contributor or agent reads first says Python 3.12 only, and section 0 names the new guarantees (D-09)</name>
  <files>README.md, AGENTS.md, docs/CODING_VALUES.md, docs/HOW_TO_DEVELOP.md, docs/architecture/overview.md</files>
  <read_first>
    - `README.md` lines 92-100 ("Without Docker") and 195-216 ("Development")
    - `AGENTS.md` "Stack" section (line 21; `CLAUDE.md` is a symlink to this file -- edit `AGENTS.md`, never the link)
    - `docs/CODING_VALUES.md` line 31 (Stack) and lines 189-193 (Project-specific)
    - `docs/HOW_TO_DEVELOP.md` section 0 (lines 15-33; Russian)
    - `docs/architecture/overview.md` line 20
    - `docs/architecture/decision_log.md` L22 and L23 (what the edits cite)
  </read_first>
  <action>
Surgical edits (CLAUDE.md): change only the version claim and its citation, and keep each
sentence's shape and each file's language.
- `README.md`: the line before the "Without Docker" commands becomes `Python 3.12:`. The
  `make venv` comment says it needs CPython 3.12. The sentence on where `make verify`
  runs says CI runs it on Python 3.12. The bold wheels sentence says `cadquery-ocp`
  publishes wheels up to CPython 3.12 and spur supports 3.12 only (L23). Keep the rest
  of that paragraph.
- `AGENTS.md` Stack: Python 3.12 only (L23), keeping the `cadquery-ocp` parenthetical.
  One line. The file's length rule ("keep this file short") holds.
- `docs/CODING_VALUES.md`: the Stack bullet says Python 3.12, with "Why →" citing `L01`
  and `L23`. The Project-specific bullet becomes **Python 3.12 only** (L23): keep the
  wheels reason and the `make venv` sentence; the closing warning says not to lower the
  floor without a real interpreter for the new floor in CI (L23), and not to raise the
  ceiling without checking wheels.
- `docs/HOW_TO_DEVELOP.md` section 0, in Russian: the CI bullet says Python 3.12. Add one
  bullet naming the three new guarantees, keeping the terms `commit-msg` and "ruleset" as
  written here (section 8 uses them): the `commit-msg` hook refuses GitHub Actions skip
  tokens; the ruleset on `main` makes GitHub refuse a merge without green `test (3.12)`,
  `vendor-bundle` and `image` on an up-to-date head, and any direct push (D-12); and
  `make pr.land PR=N` is the path into `main`, which also checks the squash text and the
  squash commit's run (L22, section 8).
- `docs/architecture/overview.md`: the Core / API bullet says Python 3.12, with "Why →"
  citing `L01` and `L23`.

**Commit** with plain `git commit`, for example
`docs(05-05): contributor docs say Python 3.12 only`.
  </action>
  <verify>
    <automated>grep -q 'Python 3.12' README.md && grep -q 'L23' AGENTS.md && grep -q 'L23' docs/CODING_VALUES.md && grep -q 'L23' docs/architecture/overview.md && S0=$(awk '/^## 0\./,/^## 1\./' docs/HOW_TO_DEVELOP.md) && printf '%s\n' "$S0" | grep -q 'make pr.land' && printf '%s\n' "$S0" | grep -qi 'ruleset' && printf '%s\n' "$S0" | grep -q 'commit-msg'</automated>
    <fails_when>non-zero exit -- one of the five files lacks its new claim or citation, or section 0 of HOW_TO_DEVELOP does not name the hook, the ruleset and `make pr.land`</fails_when>
    <automated>grep -n -e 'Python 3\.10' -e '3\.10-3\.12' -e '3\.10–3\.12' -e '3\.10 and 3\.12' -e '3\.10 и 3\.12' -e '3\.10+' README.md AGENTS.md docs/CODING_VALUES.md docs/HOW_TO_DEVELOP.md docs/architecture/overview.md; test $? -eq 1</automated>
    <fails_when>non-zero exit -- a line still claims 3.10 support (the line is printed above), or grep itself failed (status 2)</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - The five files say Python 3.12 only; AGENTS.md, CODING_VALUES.md and overview.md cite L23
    - HOW_TO_DEVELOP section 0 names the commit-msg hook, the ruleset on `main` and `make pr.land`, in Russian
    - `CLAUDE.md` is still a symlink to `AGENTS.md` (`test -L CLAUDE.md`)
    - `git show --stat --format= HEAD` lists exactly the five files in `<files>`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The first things anyone reads state the supported interpreter truthfully, and section 0 lists the merge gate among the guarantees. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: The architecture docs and the codebase map say 3.12, and the "never executed" note is retired with run URLs (D-07, D-09)</name>
  <files>docs/architecture/packaging.md, .planning/codebase/STACK.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md</files>
  <read_first>
    - `docs/architecture/packaging.md` lines 44-66 (where `make verify` runs; "Platform notes")
    - `.planning/codebase/STACK.md` lines 10-30, 110-116 and 137-144
    - `.planning/codebase/CONVENTIONS.md` lines 45-55
    - `.planning/codebase/INTEGRATIONS.md` lines 62-80 ("CI/CD & Deployment")
    - `.planning/codebase/CONCERNS.md` lines 180-186 and 212-222
    - `.planning/phases/05-ci-observed-green/05-CONTEXT.md` D-07 (the two run URLs and what each proves)
  </read_first>
  <action>
Surgical, per D-09 and D-07: replace only the claim, and keep headers, bullets and
surrounding text.
- `docs/architecture/packaging.md`: CI runs `make verify` on Python 3.12; `make venv`
  picks `python3.12` itself.
- `.planning/codebase/STACK.md`: the Primary language bullet says Python 3.12 only
  (L23); its sub-bullet says ruff targets 3.12 and CI runs 3.12; the Runtime line says
  CPython 3.12; the build notes say ruff `target-version = py312`; and the Development
  requirement is Python 3.12 on PATH.
- `.planning/codebase/CONVENTIONS.md`: ruff targets Python 3.12; mypy's version is 3.12,
  the same as the runtime (L23).
- `.planning/codebase/INTEGRATIONS.md`: the workflow bullet and "Job 1 (test)" say
  Python 3.12. Add one line to the CI section: runs are observed green -- main push run
  https://github.com/halfb00t/spur/actions/runs/35963114939 -- and merges to `main` go
  through `make pr.land`, which requires every job in
  `.github/workflows/required-jobs.txt` (L22), behind a ruleset on `main` that makes
  GitHub refuse a merge without those jobs green on an up-to-date head (D-12).
- `.planning/codebase/CONCERNS.md`: "Current coverage" says the gate runs on Python 3.12
  via CI. The "Python:" note says pinned to 3.12 only (`>=3.12,<3.13`, L23). In the
  "CI:" note, the matrix wording becomes 3.12, and its "**Note:**" sentence claiming the
  workflow has not yet run inside GitHub Actions is replaced with the evidence (D-07): main push run 35963114939 (`59f02c3`,
  all four jobs green) and PR #3 head run 36088409707 (`2c4b544`, tree-identical to
  `bfc9110`); the ruleset on `main` requires `test (3.12)`, `vendor-bundle` and `image`
  green on an up-to-date head (D-12), and every merge through `make pr.land` ends with a
  run URL for the new `main` commit (L22).

**Commit** with plain `git commit`, for example
`docs(05-05): codebase map and packaging say 3.12 and cite the observed CI runs`.
  </action>
  <verify>
    <automated>grep -q '35963114939' .planning/codebase/CONCERNS.md && grep -q '36088409707' .planning/codebase/CONCERNS.md && grep -qi 'ruleset' .planning/codebase/CONCERNS.md && grep -q '35963114939' .planning/codebase/INTEGRATIONS.md && grep -q 'make pr.land' .planning/codebase/INTEGRATIONS.md && grep -qi 'ruleset' .planning/codebase/INTEGRATIONS.md</automated>
    <fails_when>non-zero exit -- a corrected CI note lacks its run evidence, the ruleset, or the merge gate</fails_when>
    <automated>grep -n -i -e 'never executed' -e 'Python 3\.10' -e '3\.10–3\.12' -e '3\.10 and 3\.12' -e 'py310' -e '3\.10+' -e 'targets 3\.10' docs/architecture/packaging.md .planning/codebase/STACK.md .planning/codebase/CONVENTIONS.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md; test $? -eq 1</automated>
    <fails_when>non-zero exit -- a line still claims 3.10 support or that the workflow never executed (printed above), or grep itself failed (status 2)</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - The five files state 3.12 only; `CONCERNS.md` and `INTEGRATIONS.md` cite run 35963114939 and name the ruleset on `main` and `make pr.land`, and `CONCERNS.md` also cites 36088409707
    - No header, bullet structure or unrelated line changed (`git show --format= HEAD` on the task commit shows only the targeted lines and the one added INTEGRATIONS line)
    - `git show --stat --format= HEAD` lists exactly the five files in `<files>`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The architecture and codebase map describe the CI that actually ran and the interpreter that actually runs. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: REQ-ci-verified's wording, PROJECT.md's runtime, and the STATE.md blocker retired with run URLs; then the phase-wide residue check (D-07, D-09, D-10)</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 145-152 (REQ-ci-verified) and the traceability table (about line 227) -- the table is not edited
    - `.planning/PROJECT.md` "Constraints" (the Runtime bullet, about line 125) -- the Key Decisions table is not edited
    - `.planning/STATE.md` "Blockers/Concerns": the four-line CI blocker bullet that opens it, and the italic resolved-items note at its end (the shape to extend)
    - `.planning/phases/05-ci-observed-green/05-CONTEXT.md` D-07 (what each run proves; this phase's own run is recorded after the merge, not now)
    - This plan's flagged assumptions 2-4
  </read_first>
  <action>
- `.planning/REQUIREMENTS.md`: in REQ-ci-verified's main bullet, "on both supported
  Python versions" becomes "on the supported Python version (3.12, L23)". The
  *Retires* and *Acceptance* sub-bullets, the checkbox and the traceability row stay as
  they are (flagged assumption 4).
- `.planning/PROJECT.md`: the Runtime constraint says Python 3.12 only -- `cadquery-ocp`
  publishes wheels for nothing newer, and the floor is L23's -- and keeps the Docker
  sentence, citing `(L01, L12, L23)`.
- `.planning/STATE.md`, one scoped edit inside "Blockers/Concerns" (flagged assumption
  3; nothing else in the file):
  - Remove the whole four-line bullet whose bold lead is the CI-unverified blocker.
  - Extend the existing italic resolved-items note with a sentence in its style:
    resolved in Phase 5, "CI workflow unverified" -- the premise was false: main push run
    https://github.com/halfb00t/spur/actions/runs/35963114939 (`59f02c3`, all four jobs
    green) and PR #3 head run https://github.com/halfb00t/spur/actions/runs/36088409707
    (`2c4b544`, tree-identical to `bfc9110`); L22's merge gate keeps it true, and this
    phase's own squash-commit run is recorded after `make pr.land` prints it.
  - Do not write any other run URL (D-07).

**Commit** with plain `git commit`, for example
`docs(05-05): retire the CI-unverified blocker with the observed run URLs`.

**Then the phase-wide residue check** (the `<verify>` commands). If a claim turns up in
a file this phase did not list, correct it in a follow-up commit in this task and name
the file in the summary. A match in history (flagged assumption 2) is expected and
excluded by the command.
  </action>
  <verify>
    <automated>grep -q 'on the supported Python version' .planning/REQUIREMENTS.md && grep -q 'L23' .planning/PROJECT.md</automated>
    <fails_when>non-zero exit -- REQ-ci-verified's wording or PROJECT.md's runtime line was not updated</fails_when>
    <automated>grep -q 'actions/runs/35963114939' .planning/STATE.md && grep -q 'actions/runs/36088409707' .planning/STATE.md && grep -n -e '^- \*\*CI workflow unverified' -e 'has never executed inside GitHub Actions' .planning/STATE.md; test $? -eq 1</automated>
    <fails_when>non-zero exit -- the blocker bullet or one of its continuation lines is still in STATE.md (printed above), or a run URL is missing from the resolved note</fails_when>
    <automated>git grep -n -e 'Python 3\.10' -e '3\.10-3\.12' -e '3\.10–3\.12' -e '3\.10 and 3\.12' -e '3\.10 и 3\.12' -e 'python3\.1[01]' -e 'py310' -e '3\.10+' -e 'never executed' -e 'free.plan' -e 'no branch protection' -- . ':!docs/architecture/decision_log.md' ':!docs/tech_debt/resolved' ':!.planning/phases' ':!.planning/milestones' ':!.planning/intel' ':!.planning/ROADMAP.md' ':!docs/plan-2026-09-21.md' ':!docs/review-2026-09-21.md' ':!.planning/quick' > "${TMPDIR:-/tmp}/p5-residue.txt"; RC=$?; grep -v '^\.planning/PROJECT\.md:[0-9]*:. L01 ' "${TMPDIR:-/tmp}/p5-residue.txt"; test "$RC" -le 1 && ! grep -qv '^\.planning/PROJECT\.md:[0-9]*:. L01 ' "${TMPDIR:-/tmp}/p5-residue.txt"</automated>
    <fails_when>non-zero exit -- a tracked file outside the excluded history still claims 3.10 support, that CI never executed, or that the repository sits on a plan without branch protection (the lines are printed), or `git grep` itself failed (status 128)</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - REQ-ci-verified's main bullet says "on the supported Python version (3.12, L23)"; its checkbox and traceability row are unchanged
    - PROJECT.md's Runtime constraint says 3.12 only and cites L23; the Key Decisions table is unchanged
    - STATE.md's Blockers/Concerns no longer holds the CI blocker bullet or any of its continuation lines; the resolved note cites both run URLs; the other blocker bullets are byte-identical
    - STATE.md contains no run URL other than the two cited (this phase's own run is recorded after the merge)
    - The residue check prints nothing
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The blocker that opened this milestone is retired with the run URLs that disprove it, the requirement reads true, and no current document contradicts the 3.12-only code. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record → the next session | STATE.md and the codebase map are what the next agent believes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-18 | Repudiation | `.planning/STATE.md` recording evidence | medium | mitigate | Only the two runs that exist are cited; this phase's own run is recorded after `make pr.land` prints it (D-07; prohibition). |
| T-05-19 | Tampering | `.planning/STATE.md` structure | low | mitigate | One scoped edit inside Blockers/Concerns instead of the line-based `state.resolve-blocker`, which would orphan the bullet's continuation lines (flagged assumption 3); the verify checks the lines are gone. |
</threat_model>

<verification>
- Every current document says Python 3.12 only; the residue check prints nothing
- The CI notes cite runs 35963114939 and 36088409707 and name the ruleset on `main`
- Section 0 of HOW_TO_DEVELOP names the hook, the ruleset and `make pr.land`
- The STATE.md blocker is retired with both URLs
- `make verify` exits 0

Feedback latency (accepted on purpose): each task's first automated check -- a
`grep` -- is its fast signal, well under 30 s. The trailing `make verify` (~42 s) stays:
L13 makes it the one gate, and the pre-commit hook already runs it on every commit, so it
adds no wait the commit has not paid.
</verification>

<success_criteria>
ROADMAP SC-3 holds in full: the README and every current document agree with
`requires-python`, the ruff target, the CI matrix, the Makefile and L23. SC-4's blocker
half holds: "CI workflow unverified" is retired citing runs 35963114939 and 36088409707.
The record names the wall D-12 put up. Together with Plans 05-01 to 05-04, all five
success criteria of the reframed Phase 5 are met (D-10, D-12).
</success_criteria>

<output>
Create `.planning/phases/05-ci-observed-green/05-05-SUMMARY.md` when done.

**After the phase (ship and land, not part of execution).** These steps are recorded
here so they are not re-derived:
1. `/gsd-ship 5`. The ship-note commit is refused by the new hook, so commit
   `.planning/STATE.md` by hand as `docs(05): ship phase 5 — PR #M` and push
   (HOW_TO_DEVELOP section 6).
2. Before landing, check the PR title and body. They name skip tokens only in words:
   `gsd-ship` builds the body from planning artifacts that write the tokens literally,
   and `make pr.land` refuses such a body (edit it on GitHub, then retry).
3. Wait for the PR head's run, then `make pr.land PR=M`. It must end with a run URL for
   the squash commit. Record that URL, and the seconds it took to appear (05-02 flagged
   assumption 6), on the next STATE.md touch (D-07). This is also the first merge
   through the ruleset on `main` (D-12): if `gh pr merge` is refused by a rule, pr.land
   prints GitHub's message and exits non-zero with nothing merged -- read it before
   changing anything.
4. RESEARCH A2, falsified once: `git log -1 --format=%B {squash sha}` must show the
   subject `{PR title} (#M)` followed by the PR body. The failure signal is a body that
   is a list of `* {commit subject}` lines, or a subject without `(#M)`.
5. `/gsd-complete-milestone` (milestone v0.1 ends with this phase) commits its archive
   on a local `main`. The ruleset refuses a direct push to `main` (D-12, 05-03 flagged
   assumption 3), so those commits go up on a branch, as a PR, landed with
   `make pr.land`.
</output>
