---
phase: 05-ci-observed-green
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - docs/HOW_TO_DEVELOP.md
  - docs/architecture/packaging.md
  - docs/architecture/decision_log.md
autonomous: true
requirements: [REQ-ci-verified]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A ruleset on `main` requires `test (3.12)`, `vendor-bundle` and `image` to be green on an up-to-date head and requires a pull request, read back via `gh api repos/halfb00t/spur/rules/branches/main` (D-12, ROADMAP SC-5). It is the repository's existing ruleset `default` (id 23977515) retargeted to `refs/heads/main`, not a second ruleset: the read-back lists exactly the rule types `deletion`, `non_fast_forward`, `pull_request` and `required_status_checks`, every one from ruleset 23977515, with the strict up-to-date policy on and each required check pinned to GitHub Actions (integration 15368)"
    - "The ruleset never names `test (3.10)`: it names the post-D-09 set from the start and is applied here, before Plan 05-04 collapses the matrix. That is safe because `main`'s current `ci.yml` already reports all three names on every PR (run 36088409707), while naming the 3.10 job would leave every PR after this phase waiting for a check no job reports (D-12 ordering)"
    - "`docs/HOW_TO_DEVELOP.md` section 8 records, next to the D-03 PATCH, the exact command that applied the ruleset, the read-back command, the one call that removes it, and that the command is re-run whenever `required-jobs.txt` changes -- because a repository setting is not in git (D-12)"
    - "Section 8 names `make pr.land PR=N` as the way a phase lands on `main` (D-05, ROADMAP SC-2): what it checks, in order; that every refusal means nothing was merged; that it does the local follow-up itself and deletes the local branch only when its tip is the merged head; and what to do if it ends with 'merged, but no run appeared'. Written in Russian, like the rest of the file"
    - "Section 8 says what the wall is and what the tool is (D-12; the human's wording 'a tool, not a wall' is kept, for pr.land): the ruleset makes GitHub refuse a merge of a head without green required checks or behind `main`, and any direct push; `make pr.land` is the sanctioned path through it because only it refuses a skip token in the squash text and proves a run appears for the squash commit. The web button merges only what the ruleset accepts but skips those two checks, so it is not the path"
    - "No instruction to push straight to `main` survives in `docs/HOW_TO_DEVELOP.md`, because the ruleset refuses it (D-12): section 2 says the phase branch is cut from `origin/main` before discussion and that discussion and plan commits ride it (Phase 5's `956419a`…`40fcaaa` are on `gsd/phase-05-ci-observed-green`, none on `main`); 'Параллельные циклы' says work landed with `make worktree.land` reaches `main` through a PR"
    - "The squash-setting command added by Plan 05-01, and the rule that red CI or open review findings mean no merge, are kept in section 8; the manual `git switch main && git pull --ff-only …` block is replaced by a description of what pr.land does locally"
    - "`docs/architecture/packaging.md` says that merges to `main` go through `make pr.land` and that the ruleset on `main` requires the same jobs green, next to the paragraph listing where `make verify` runs"
    - "`docs/architecture/decision_log.md` gains `## L22` (dated 2026-09-25, appended, nothing above it changed): the trigger (PR #2 merged five minutes after run 35993984796 failed `test (3.10)`; the run-less squash commits `538d26f` and `bfc9110`), then the commit-msg hook (D-02), the squash message (D-03), the ship-note step (D-04), `make pr.land` (D-05) and the ruleset on `main` (D-12, which superseded D-06), each with its evidence, the ruleset's read-back included; it records that GitHub's documentation states no case rule for the tokens (RESEARCH A1), and it extends L13 without superseding it (D-08)"
    - "`make verify` passes (L13)"
  prohibitions:
    - statement: "No document claims more wall, or less, than exists. Section 8, packaging.md and L22 describe the ruleset as read back from `rules/branches/main` -- what GitHub refuses -- and say what it cannot do, which only `make pr.land` does: the token check on the squash text and the run for the squash commit (D-12). Checked by reading the three texts against the read-back recorded in `05-03-SUMMARY.md`."
      status: resolved
      verification: judgment
    - statement: "The ruleset never requires a check no job reports: its required contexts are exactly `image`, `test (3.12)` and `vendor-bundle`. Backed by the read-back assertion in Task 1, and again after the matrix collapses (Plan 05-04 Task 3)."
      status: resolved
      verification: test
    - statement: "The decision log is append-only: L01-L21 stay byte-identical, and L22 supersedes no log entry (D-08). Backed by the task's diff check -- the L22 commit removes no line from the log."
      status: resolved
      verification: test
  artifacts:
    - path: "docs/HOW_TO_DEVELOP.md"
      provides: "Section 8: the ruleset's apply, read-back and removal commands; `make pr.land PR=N` as the merge path; the wall-and-tool paragraph. Section 2 and 'Параллельные циклы' without a direct push to `main`"
      contains: "make pr.land PR="
    - path: "docs/architecture/packaging.md"
      provides: "The merge gate named next to the gate's three places"
      contains: "make pr.land"
    - path: "docs/architecture/decision_log.md"
      provides: "L22, the merge gate"
      contains: "## L22"
  key_links:
    - from: "docs/HOW_TO_DEVELOP.md"
      to: "GitHub ruleset 23977515, targeting main"
      via: "section 8 holds the exact command that applied it and the read-back that proves it"
      pattern: "rulesets/23977515"
    - from: "docs/HOW_TO_DEVELOP.md"
      to: ".github/workflows/required-jobs.txt"
      via: "section 8 names the list pr.land requires, and says to re-run the ruleset command when it changes"
      pattern: "required-jobs.txt"
    - from: "docs/architecture/decision_log.md"
      to: "scripts/pr_land.py"
      via: "L22 describes the tool by its target and its steps"
      pattern: "make pr.land"
---

<objective>
Put the wall up, make the merge gate the documented path, and record the decision.

**Task 1.** The ruleset on `main` (D-12, which supersedes D-06). The repository's own,
untargeted ruleset `default` is retargeted to `main`. It gains required status checks
`test (3.12)`, `vendor-bundle` and `image` on an up-to-date head, and keeps its
pull-request rule. The command goes into `docs/HOW_TO_DEVELOP.md` section 8 next to the
D-03 PATCH, and `rules/branches/main` is read back as the evidence (ROADMAP SC-5).

**Task 2.** Section 8 is rewritten around `make pr.land PR=N` (D-05). It says what the
tool checks and does, and what the wall does and does not do: pr.land is "a tool, not a
wall", the sanctioned path through the ruleset (D-12). Section 2 and "Параллельные
циклы" stop telling anyone to push to `main`, which the ruleset now refuses.
`docs/architecture/packaging.md` names the merge gate beside the gate itself.

**Task 3.** `L22` is appended to the decision log (D-08): the hook, the squash message,
the ship-note step, `make pr.land` and the ruleset, as one decision. Its trigger is PR
#2's red merge and the two run-less squash commits.

Purpose: GitHub itself refuses a red, check-less or stale merge into `main`. The human
who merges reads one procedure. The next agent who wonders why `main` refuses a push finds
the reason in the log instead of re-deriving it.

Output: the ruleset on `main`, read back; section 8 rewritten and sections 2 and
"Параллельные циклы" corrected (Russian); one paragraph in `packaging.md`; `L22`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-observed-green/05-CONTEXT.md
@.planning/phases/05-ci-observed-green/05-01-SUMMARY.md
@.planning/phases/05-ci-observed-green/05-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates:

- GitHub repository setting (not in git): ruleset `default`, id 23977515, targeting
  `refs/heads/main`, with the rules `deletion`, `non_fast_forward` and `pull_request` as
  the human configured them, plus `required_status_checks` (`test (3.12)`,
  `vendor-bundle`, `image`, each from integration 15368, strict policy on)
- `## L22` in `docs/architecture/decision_log.md`
- a rewritten section 8 "Влить", and corrected section 2 and "Параллельные циклы", in
  `docs/HOW_TO_DEVELOP.md`
- one paragraph in `docs/architecture/packaging.md`

No code symbols. The rest of the phase: `05-01` (`scripts/skip_tokens.py`, the hook, the
squash setting), `05-02` (`scripts/pr_land.py`, `make pr.land`,
`.github/workflows/required-jobs.txt`), `05-04` (`L23`, Python 3.12 only), `05-05` (the
record made true).
</artifacts_this_phase_produces>

<flagged_assumptions>
1. **Retarget `default`; do not create a second ruleset** (D-12 leaves this to the
   planner). Planning read it on 2026-09-25 (`gh api repos/halfb00t/spur/rulesets/23977515`).
   It was created at 03:50 UTC, minutes after the repository went public. It has
   `enforcement: active` and `bypass_actors: []`. Its rules are `deletion`,
   `non_fast_forward` and `pull_request` (0 required approvals, merge methods
   merge/squash/rebase, thread resolution off). Its `conditions.ref_name.include` is `[]`,
   so it applies to no branch and `rules/branches/main` is `[]`. It is the human's own
   half-built wall: it already requires a pull request, and what it lacks is a target and
   the required checks. Retargeting leaves one ruleset on `main` with the human's rules as
   set. A second ruleset would leave an inert `default` beside it for the next reader to
   puzzle over. The apply command builds the PUT body from the live GET, so the human's
   rules travel unchanged. That includes `require_extra_approval_for_unattributed_changes:
   true`, which the live API returns but GitHub's REST reference (fetched 2026-09-25) does
   not list. GitHub's rules page says it only adds an approval when Copilot opens a PR that
   no person is attributed to; this repository's PRs are opened by `gsd-ship` through
   `gh`. The planning dry run built the body from the live GET without sending it. The
   name `default` is kept (not asked to change).

2. **The ruleset never names `test (3.10)`, and it is applied here, before the matrix
   collapses** (D-12's ordering choice). Its three contexts are the post-D-09 set.
   `main`'s `ci.yml` today reports all three on every PR, plus `test (3.10)`, which
   nothing requires. So a PR opened while this phase is in flight can still satisfy the
   wall. Naming the 3.10 job instead would, once this phase lands, leave every PR waiting
   for a check no job reports. Between this plan and Plan 05-04, `required-jobs.txt`
   still lists four jobs: pr.land is stricter than the wall for two plans, while nothing
   merges. Plan 05-04 Task 3 reads the ruleset back and asserts it equals the collapsed
   list.

3. **No bypass actors, so no direct push to `main`** (the planner's choice: stability
   over speed, surfaced for the human). GitHub's rules page: "You can require that all
   changes to the target branch be associated with a pull request." A bypass for the
   repository admin would let the one human who merges also merge a red PR from the
   button: the hole D-12 closes. The consequences are made true in Task 2's docs.
   Section 2's instruction to push `main` after planning is refused, and it is already
   stale: Phase 5's planning commits `956419a`…`40fcaaa` ride
   `gsd/phase-05-ci-observed-green` and none are on `main`. The branch was checked out
   from `main` before discuss-phase committed; gsd's discuss-phase and plan-phase create
   no branch, and `gsd-execute-phase` reuses an existing one. A `make worktree.land` onto
   a local `main` cannot be pushed. `/gsd-complete-milestone`, due after this phase,
   commits on local `main`, so its commits go up as a PR (05-05's after-phase steps).
   That the push is refused is GitHub's documented behaviour, not exercised here:
   exercising it would be a write attempt against `main`.

4. **The rules D-12 does not name are kept, not added.** `deletion` and
   `non_fast_forward` come with the human's `default` ruleset. Nothing else is added --
   signed commits, linear history, narrowed merge methods, required approvals -- because
   D-12 does not ask for it.

5. **pr.land keeps every pre-merge check** (05-02 flagged assumption 8). Section 8 and
   L22 say which checks the ruleset duplicates, why they stay, and which only pr.land
   makes.

6. **L22 is one entry for D-02…D-05 and D-12, not five.** CONTEXT D-08 names it that
   way, and D-12 says L22 covers the ruleset. It extends L13 ("`make verify` is the
   gate") without superseding it: L13 stays true, and L22 adds that a merge requires CI
   to have run that gate on the merged tree. D-12 superseded D-06, a CONTEXT decision,
   not a log entry, so L22's heading supersedes nothing.

7. **L22 is appended after the tools and the wall exist** (05-01, 05-02, Task 1), so it
   can describe them by name and cite the read-back. Phase 4 appended L21 before turning
   its rule on, but nothing here depends on the log being written first. Numbering
   follows the ROADMAP: L22 is the merge gate, L23 is Python 3.12 (appended in Plan
   05-04).
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: The wall -- the repository's `default` ruleset retargeted to `main`, requiring green `test (3.12)`, `vendor-bundle` and `image` on an up-to-date head and a pull request; read back and recorded (D-12)</name>
  <reversibility rating="costly">D-12's own rating: the ruleset comes off with one `gh api` call (recorded in section 8), but the documented merge path and L22 would need rewriting. The human chose it (D-12), so it is flagged, not gated.</reversibility>
  <precondition>`gh auth status` exits 0; `gh api 'repos/{owner}/{repo}' --jq .permissions.admin` prints `true`; and `gh api repos/halfb00t/spur/rulesets/23977515 --jq '[.rules[].type] | sort | join(",")'` prints `deletion,non_fast_forward,pull_request` -- the ruleset is as planning read it.</precondition>
  <files>docs/HOW_TO_DEVELOP.md</files>
  <read_first>
    - `.planning/phases/05-ci-observed-green/05-CONTEXT.md` D-12 (the amendment), D-03, and the "Verified GitHub facts" correction
    - `docs/HOW_TO_DEVELOP.md` section 8 as Plan 05-01 left it: the squash-setting paragraph and its `sh` block, which the new paragraph sits beside (Russian)
    - `.github/workflows/required-jobs.txt` and `.github/workflows/ci.yml`: the names GitHub reports for the jobs
    - This plan's flagged assumptions 1-4
  </read_first>
  <action>
**Read first, read-only, and paste both outputs into `05-03-SUMMARY.md`:**
`gh api repos/halfb00t/spur/rulesets/23977515` and
`gh api repos/halfb00t/spur/rules/branches/main`. Planning read the ruleset as flagged
assumption 1 describes, and `[]` for the branch. If the ruleset's include list is no
longer empty, or its rules differ from the precondition, stop and report: the human
changed it after planning, and the command below would overwrite that change.

**Apply it (per D-12) with exactly this command.** It reads the live ruleset, keeps every
rule except a `required_status_checks` one, adds the required checks, targets `main`,
and writes the result back with PUT:
`gh api repos/halfb00t/spur/rulesets/23977515 --jq '{conditions: {ref_name: {include: ["refs/heads/main"], exclude: []}}, rules: ([.rules[] | select(.type != "required_status_checks")] + [{type: "required_status_checks", parameters: {strict_required_status_checks_policy: true, do_not_enforce_on_create: false, required_status_checks: [{context: "test (3.12)", integration_id: 15368}, {context: "vendor-bundle", integration_id: 15368}, {context: "image", integration_id: 15368}]}}])}' | gh api -X PUT repos/halfb00t/spur/rulesets/23977515 --input -`

Why each part. `strict_required_status_checks_policy: true` is "require branches to be
up to date before merging". `integration_id: 15368` is GitHub Actions: planning read
`app.slug: github-actions`, `app.id: 15368` on every check run of `2c4b544`, so a status
of the same name from anywhere else cannot satisfy the wall. The three contexts are the
post-D-09 set (flagged assumption 2). `bypass_actors` and `enforcement` are not sent, so
the human's `[]` and `active` stay (flagged assumption 3). If the PUT answers with an
error, paste it into the summary and stop: do not reshape the body by guessing fields.

**Read it back** (the SC-5 evidence): `gh api repos/halfb00t/spur/rules/branches/main`,
and paste the output into `05-03-SUMMARY.md`. The `<verify>` assertion reduces it to the
rule types, the strict flag, the checks with their integration, and the ruleset ids.

**Section 8, in Russian, beside Plan 05-01's squash-setting paragraph:** a short
paragraph and an `sh` block. Say what the wall is: the repository's ruleset `default`
(id 23977515) on `main`. It requires `test (3.12)`, `vendor-bundle` and `image` green
from GitHub Actions on a head that is up to date with `main`, and a pull request for
every change; GitHub refuses a merge without them, and refuses a direct push (L22, D-12).
Say why the command lives here: a repository setting is not in git. The `sh` block holds
three commands: the exact apply command above; the read-back
`gh api repos/halfb00t/spur/rules/branches/main`; and the removal
`gh api -X DELETE repos/halfb00t/spur/rulesets/23977515`. Then one sentence: when
`.github/workflows/required-jobs.txt` changes, edit the contexts in the apply command to
match and run it again. It replaces the required-checks rule rather than adding a second
one, and until it is re-run the wall and `make pr.land` require different jobs. Leave the
rest of section 8 for Task 2.

**Commit** with plain `git commit`, for example
`docs(05-03): record the ruleset that walls main behind green CI (D-12)`.
  </action>
  <verify>
    <automated>test "$(gh api repos/halfb00t/spur/rules/branches/main --jq '[([.[].type] | sort), [.[] | select(.type == "required_status_checks") | .parameters.strict_required_status_checks_policy], ([.[] | select(.type == "required_status_checks") | .parameters.required_status_checks[] | "\(.context)@\(.integration_id)"] | sort), ([.[].ruleset_id] | unique)] | tostring')" = '[["deletion","non_fast_forward","pull_request","required_status_checks"],[true],["image@15368","test (3.12)@15368","vendor-bundle@15368"],[23977515]]'</automated>
    <fails_when>non-zero exit -- the rules on `main` are not exactly deletion, non-fast-forward, pull-request and required-checks, all from ruleset 23977515; or the strict up-to-date policy is off; or the required checks are not exactly `image`, `test (3.12)` and `vendor-bundle`, each from GitHub Actions (15368); or `gh` failed</fails_when>
    <automated>grep -q 'rulesets/23977515' docs/HOW_TO_DEVELOP.md && grep -q 'rules/branches/main' docs/HOW_TO_DEVELOP.md && grep -q 'squash_merge_commit_message=PR_BODY' docs/HOW_TO_DEVELOP.md && test "$(grep -o 'context: "[^"]*"' docs/HOW_TO_DEVELOP.md | LC_ALL=C sort | tr '\n' ';')" = 'context: "image";context: "test (3.12)";context: "vendor-bundle";'</automated>
    <fails_when>non-zero exit -- section 8 lacks the apply command, the read-back, or 05-01's squash-setting command, or the recorded apply command names checks other than the three</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `05-03-SUMMARY.md` holds the two reads taken before the change, the PUT's outcome, and the read-back's full output
    - The read-back shows the rule types `deletion`, `non_fast_forward`, `pull_request` and `required_status_checks`, all from ruleset 23977515; the strict policy on; the required checks exactly `image`, `test (3.12)` and `vendor-bundle` at integration 15368
    - Section 8, in Russian, holds the exact apply command, the read-back and the removal call next to the squash-setting command, and says to re-run the apply command when `required-jobs.txt` changes
    - `git show --stat --format= HEAD` lists exactly `docs/HOW_TO_DEVELOP.md`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>GitHub refuses to merge into `main` a head without green `test (3.12)`, `vendor-bundle` and `image`, or behind `main`, and refuses a direct push; the setting is read back, and the command that made it is in the docs. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Section 8 is `make pr.land PR=N`, the sanctioned path through the wall -- "a tool, not a wall"; no instruction to push to `main` survives (D-05, D-12)</name>
  <files>docs/HOW_TO_DEVELOP.md, docs/architecture/packaging.md</files>
  <read_first>
    - `docs/HOW_TO_DEVELOP.md` (whole file; Russian): section 8 as Plan 05-01 and Task 1 left it (the squash-setting and ruleset paragraphs), section 6 (the ship-note step 05-01 added), section 2 (the paragraph on where plan commits go), "Параллельные циклы", the terse imperative style and the "short paragraph, then an `sh` block" habit of sections 6 and 8
    - `scripts/pr_land.py` module docstring and `land()`: the steps and messages to describe; do not describe behaviour the code does not have
    - `.github/workflows/required-jobs.txt`
    - `docs/architecture/packaging.md` lines 38-72: the paragraph on where `make verify` runs, and the worktree paragraph
    - `.planning/phases/05-ci-observed-green/05-03-SUMMARY.md` (Task 1): the ruleset as read back -- describe that, not what was intended
    - `.planning/phases/05-ci-observed-green/05-CONTEXT.md` D-05, D-12, `<specifics>` "Wording the user wants kept"
    - This plan's flagged assumptions 3 and 5, and `05-02-PLAN.md` flagged assumption 8
  </read_first>
  <action>
**Section 8 "Влить", in Russian** (per D-05 and D-12):
- Replace the opening sentence, the one that sends the human to GitHub's merge button,
  with: a phase lands on `main` through `make pr.land PR=N` (L22), with an `sh` block
  holding `make pr.land PR=N`.
- A short list of what it does, in order, matching `scripts/pr_land.py`. It reads the
  PR's current head. It refuses a PR that is not open or not based on `main`, a dirty
  tree, a head behind `main` (rebase, push, let CI run, retry) or identical to it, a
  head without a finished `ci.yml` run green on every job in
  `.github/workflows/required-jobs.txt`, and a PR title or body carrying a skip token
  (after D-03 that text becomes `main`'s commit message). It squash-merges exactly the
  checked head with the checked title and body. It waits up to a minute for a CI run on
  the new `main` commit and prints its URL. It switches to `main`, pulls, and deletes the
  local phase branch only if its tip is the merged head.
- Any refusal means nothing was merged.
- If it ends with "merged, but no run appeared": a skip token reached `main`, which is a
  regression of L22. Find it with `git log -1 --format=%B {sha}` and file it in
  `docs/tech_debt/`.
- **The wall and the tool** (D-12). Keep the human's words "инструмент, а не стена", for
  pr.land. The wall is the ruleset in Task 1's paragraph, which stays where it is: GitHub
  itself refuses a merge whose head lacks green required checks or is behind `main`, and
  any direct push. `make pr.land` is a tool, not a wall, and it is the sanctioned path
  through the wall because it does what the wall cannot. It refuses a skip token in the
  PR title or body (after D-03 that text becomes `main`'s commit message, so the token
  acts after the wall). It proves a run appears for the squash commit. It does the local
  follow-up. It also checks the job list and the behind count itself, so its claim that
  the merged tree is the checked tree never rests on a setting outside git (05-02
  flagged assumption 8). The web button merges only what the wall accepts, but skips
  those checks, so it is not the path.
- Keep Plan 05-01's squash-setting paragraph and command, Task 1's ruleset paragraph and
  commands, and the existing paragraph saying red CI or open review findings mean no
  merge. The old manual `git switch main && git pull --ff-only …` block goes: pr.land
  does it, as the list says. Keep the explanation of why a squashed branch needs `-D`,
  reworded as the reason pr.land checks the tip first.

**Section 2, in Russian** (per D-12, flagged assumption 3). Replace its second paragraph,
the one saying plan commits land on `main` and that `main` must be pushed before
`gsd-execute-phase`. Say instead: `main` takes changes only through a pull request (the
ruleset, L22). So the phase branch is cut from `origin/main` before `gsd-discuss-phase`
(`git switch -c gsd/phase-NN-<slug> origin/main`), and the discussion and plan commits
ride it; `gsd-execute-phase` reuses the branch that exists. Phase 5 went this way: its
discussion, research and plan commits are on its branch, and none are on `main`. Describe
the refused push in words ("прямой push в `main`"); do not write the push command
itself, because the `<verify>` greps for it.

**"Параллельные циклы", in Russian:** one sentence after the `make worktree.land`
description. It commits on the worktree's base branch. When that base is `main`, the
result cannot be pushed, because the ruleset refuses a direct push (L22): take the base
from a branch, and bring it into `main` as a PR through `make pr.land`.

**`docs/architecture/packaging.md`**: after the paragraph listing where `make verify`
runs, add one paragraph (English). Merges to `main` go through `make pr.land PR=N`,
which refuses a PR whose head lacks a green CI run of every job in
`.github/workflows/required-jobs.txt` or is behind `main`, and confirms that a run
appears for the squash commit (L22). The ruleset on `main` makes GitHub refuse the same
red or stale merge, and any direct push (D-12); pr.land is the tool that also checks what
the ruleset cannot see. Leave that file's Python version lines to Plan 05-05.

**Commit** with plain `git commit`, for example
`docs(05-03): the merge path is make pr.land, through the ruleset on main`.
  </action>
  <verify>
    <automated>grep -q 'make pr.land PR=' docs/HOW_TO_DEVELOP.md && grep -q 'required-jobs.txt' docs/HOW_TO_DEVELOP.md && grep -qi 'не стена' docs/HOW_TO_DEVELOP.md && grep -q 'squash_merge_commit_message=PR_BODY' docs/HOW_TO_DEVELOP.md && grep -q 'rulesets/23977515' docs/HOW_TO_DEVELOP.md</automated>
    <fails_when>non-zero exit -- section 8 lacks the command, the job list, the tool-not-wall wording, 05-01's squash-setting command, or Task 1's ruleset command</fails_when>
    <automated>grep -n -e 'Squash-merge PR на GitHub' -e 'Коммиты плана ложатся в' -e 'push origin main' docs/HOW_TO_DEVELOP.md; test $? -eq 1</automated>
    <fails_when>non-zero exit -- section 8 still leads with GitHub's button, or section 2 still tells anyone to push `main` (the lines are printed above), or grep itself failed (status 2)</fails_when>
    <automated>grep -q 'make pr.land' docs/architecture/packaging.md && grep -qi 'ruleset' docs/architecture/packaging.md</automated>
    <fails_when>non-zero exit -- packaging.md does not name the merge gate or the ruleset</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Section 8 is in Russian, leads with `make pr.land PR=N`, lists its checks in the order `scripts/pr_land.py` performs them, and says a refusal means nothing was merged
    - Section 8 says what the wall (the ruleset, as Task 1 read it back) refuses and what only pr.land does, keeps "инструмент, а не стена" for pr.land, and says the web button skips pr.land's checks
    - Section 8 keeps the squash-setting command, the ruleset commands and the "red CI or open review findings: no merge" rule
    - Section 2 says to cut the phase branch before discussion and has no instruction to push `main`; "Параллельные циклы" says a `worktree.land` result reaches `main` through a PR
    - `docs/architecture/packaging.md` names `make pr.land`, the ruleset and L22
    - `git show --stat --format= HEAD` lists exactly `docs/HOW_TO_DEVELOP.md` and `docs/architecture/packaging.md`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The human who merges reads one true procedure: the wall GitHub enforces, `make pr.land PR=N` as the path through it, what it refuses and proves, and that nothing reaches `main` except as a PR. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: `L22` appended -- CI is the merge gate for `main` (D-08, D-12)</name>
  <reversibility rating="one-way">`docs/architecture/decision_log.md` is append-only by policy -- a wrong entry is superseded by a later one, never edited. The human decided to append L22 (D-08), so there is no checkpoint.</reversibility>
  <files>docs/architecture/decision_log.md</files>
  <read_first>
    - `docs/architecture/decision_log.md`: the header's append-only rule (lines 1-10); `L13` (lines 118-126), which L22 extends; `L21` (line 382 to the end), the most recent entry, for the `Date:` line, the bolded sub-decision paragraphs and the closing `Reason:` paragraph naming the most likely future regression
    - `.planning/phases/05-ci-observed-green/05-CONTEXT.md` D-02 to D-05, D-08, D-12 (which superseded D-06), and `<specifics>` (the table of commits and runs)
    - `.planning/phases/05-ci-observed-green/05-01-SUMMARY.md` and `05-02-SUMMARY.md`: what was actually built, the planning-probe findings, the squash-setting values read back
    - `.planning/phases/05-ci-observed-green/05-03-SUMMARY.md` (Tasks 1-2): the ruleset before the change and as read back
    - `.planning/phases/05-ci-observed-green/05-RESEARCH.md` "Assumptions Log" A1-A3
    - This plan's flagged assumptions 1-7, and `05-02-PLAN.md` flagged assumption 8
  </read_first>
  <action>
Append, after `L21`, one entry headed
`## L22 — CI is the merge gate: a ruleset walls main, every commit on main has a run, and main is landed with make pr.land`
(put `make pr.land` in backticks), with `Date: 2026-09-25.` on its own line. Per D-08:
append only. L13 and every earlier entry stay byte-identical, and the heading carries no
"(supersedes …)", because L22 extends L13 rather than replacing it.

Content, in the L21 style (a lead paragraph, then bolded sub-decision paragraphs, then
`Reason:`):
- **Lead: the trigger.** L13 made `make verify` the gate, but nothing tied a merge to CI
  having run it. PR #2 was merged five minutes after run 35993984796 failed
  `test (3.10)`. Two squash commits on `main`, `538d26f` and `bfc9110`, have no run,
  because a skip token in a branch commit rode into GitHub's default squash message.
- **The commit-msg hook** (D-02). The six tokens are refused anywhere in the message,
  case-insensitively, above git's verbose-mode scissors line. Why a hook and not only
  the setting: PR #2's head `20b63e4` showed zero checks, and `6fce500`'s prose mention
  was enough. GitHub's documentation states no case rule, so case-insensitivity is the
  hook's deliberate over-match (RESEARCH A1). The `verify` hook is pinned to the
  pre-commit stage (planning probe: it otherwise ran twice per commit).
- **The squash message is the PR title and body** (D-03). The two setting values; the
  command lives in HOW_TO_DEVELOP section 8, because a setting is not in git.
- **The ship note** (D-04). `/gsd-ship`'s token-carrying ship note is refused, so it is
  a documented manual commit. The global workflow is not patched.
- **`make pr.land` is the sanctioned path** (D-05). Its steps. The required list sits
  next to `ci.yml` and a test keeps them equal. `--match-head-commit` binds the merge to
  the checked sha. It passes the checked subject and body itself, so RESEARCH A2 is off
  its path, and it refuses a token in them. The behind check makes the merged tree the
  checked tree (`bfc9110` and `2c4b544` share tree `6ebbeaa2…`), which is why it waits
  for the run to appear and not to finish. Local follow-up, and no `--delete-branch`.
- **The wall: the ruleset on `main`** (D-12, which superseded D-06). CONTEXT.md recorded
  a private free-plan repository and D-06 deferred the wall; by 2026-09-25 the
  repository was public and the human chose to put the wall up in this phase. What it
  is: the repository's own `default` ruleset (id 23977515), retargeted to
  `refs/heads/main` rather than a second one created (flagged assumption 1); its rules as
  Task 1 read them back from `rules/branches/main` -- the required checks `test (3.12)`,
  `vendor-bundle` and `image` from GitHub Actions (integration 15368) on an up-to-date
  head, a pull request for every change, no deletion, no force push; no bypass actors, so
  no direct push to `main` and planning rides the phase branch (flagged assumption 3). It
  never named `test (3.10)` (flagged assumption 2). The apply command lives in
  HOW_TO_DEVELOP section 8, and is re-run whenever `required-jobs.txt` changes.
- **And `make pr.land` is still the path -- a tool, not a wall.** The wall cannot see the
  squash text, where a skip token acts after the merge, or the squash commit's own run;
  pr.land checks both, and does the local follow-up. It keeps its own job and behind
  checks although the ruleset duplicates them: its "merged tree is the checked tree"
  claim should not rest on a setting outside git that it does not read (05-02 flagged
  assumption 8).
- **Reversibility.** The entry is one-way (the log is append-only). The mechanisms are
  cheap to undo: remove the hook, PATCH the setting back, DELETE the ruleset, stop using
  the target. The ruleset is D-12's "costly": one call removes it, but section 8 and
  this entry would need rewriting.
- **Reason:** name the single most likely regression. Someone re-adds a skip token "to
  save a pipeline" (the ship workflow's own default), or renames or adds a CI job
  without re-running the ruleset command -- a rename leaves every PR waiting for a check
  no job reports, an addition leaves the button weaker than pr.land. The answer on file:
  the hook refuses the first; the drift test keeps `required-jobs.txt` equal to
  `ci.yml`, and section 8 says to re-run the ruleset command with it; and `make pr.land`
  exits non-zero when a squash commit gets no run.

**Commit** with plain `git commit`, for example
`docs(05-03): L22 -- CI is the merge gate for main`. Describe the tokens in words in the
commit message; never write one.
  </action>
  <verify>
    <automated>grep -nE '^## L22 ' docs/architecture/decision_log.md</automated>
    <fails_when>exit 1 -- no L22 heading</fails_when>
    <automated>(for s in 35993984796 538d26f bfc9110 20b63e4 6fce500 'does not state a case rule' 'make pr.land' 'required-jobs.txt' 'match-head-commit' 23977515 'rules/branches/main' D-12 'Date: 2026-09-25'; do awk '/^## L22 /,0' docs/architecture/decision_log.md | grep -q -- "$s" || { echo "L22 lacks: $s"; exit 1; }; done)</automated>
    <fails_when>non-zero exit, with a `L22 lacks:` line naming the missing evidence</fails_when>
    <automated>C=$(git log -1 --format=%H -- docs/architecture/decision_log.md) && D=$(git show --format= "$C" -- docs/architecture/decision_log.md) && test -z "$(printf '%s\n' "$D" | grep -E '^-[^-]')"</automated>
    <fails_when>non-zero exit -- the L22 commit removed or changed a line of the log (append-only broken), or the commit could not be read</fails_when>
    <automated>make verify</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/architecture/decision_log.md` ends with `## L22 …`, dated 2026-09-25, with no "(supersedes" in its heading
    - L22 cites runs and commits 35993984796, `538d26f`, `bfc9110`, `20b63e4`, `6fce500`, the tree identity `6ebbeaa2`, and names D-02 to D-05 and D-12 (saying D-12 superseded D-06)
    - L22 says GitHub's documentation does not state a case rule, describes the ruleset (id 23977515) as read back from `rules/branches/main`, and says which pre-merge checks pr.land keeps and why
    - The L22 commit removes no line from the log
    - `git show --stat --format= HEAD` lists exactly `docs/architecture/decision_log.md`
    - `make verify` exits 0
  </acceptance_criteria>
  <done>The merge gate is a logged decision with its trigger, evidence, limits and the regression it forecloses. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → the human who merges | The docs decide what the human believes GitHub will and will not refuse |
| developer shell → GitHub repository ruleset | `gh api -X PUT` changes a server-side rule that is not in git and that decides every merge into `main` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13 | Repudiation | `docs/architecture/decision_log.md` | low | mitigate | Append-only, checked by the task's diff check: the L22 commit removes no line. |
| T-05-14 | Spoofing | Docs misstating the gate -- claiming GitHub refuses what it does not, or that the button is the path | medium | mitigate | Section 8, packaging.md and L22 describe the ruleset as Task 1 read it back from `rules/branches/main`, and say what only pr.land checks (the squash text, the squash commit's run). |
| T-05-21 | Elevation of Privilege | `gh api -X PUT repos/halfb00t/spur/rulesets/23977515` | medium | mitigate | Run once by the repository owner, admin permission read first (precondition); the body is built from the live GET so the human's rules travel unchanged, and the task stops if the ruleset differs from planning's read; the exact command, the read-back and the removal call are recorded in section 8; the read-back is asserted. |
| T-05-22 | Spoofing | A status or check run named like a required job, posted by something other than `ci.yml` | medium | mitigate | Every required check carries `integration_id: 15368` (GitHub Actions), asserted in the read-back, so only an Actions check run satisfies it. |
| T-05-23 | Denial of Service | A required check that no job reports, leaving every PR unmergeable | medium | mitigate | The ruleset never names `test (3.10)` (flagged assumption 2); Plan 05-04 Task 3 asserts the live required checks equal the collapsed `required-jobs.txt`; section 8 says to re-run the command whenever that list changes. |
| T-05-24 | Tampering | The ruleset removed or edited later, silently weakening the wall | low | accept | Only the admin can change it, and `make pr.land` keeps its own job and behind checks, so the sanctioned path stays exact without the wall (05-02 flagged assumption 8). Removal is D-12's documented reversal. |
</threat_model>

<verification>
- The ruleset read back from `rules/branches/main` matches exactly: rule types, strict policy, the three required checks at integration 15368, ruleset 23977515
- Section 8 holds the ruleset's apply, read-back and removal commands and the squash-setting command, leads with `make pr.land PR=N`, and has the wall-and-tool paragraph
- Section 2 and "Параллельные циклы" no longer tell anyone to push to `main`
- `packaging.md` names the merge gate and the ruleset
- L22 is appended with its evidence; the log's earlier entries are untouched
- `make verify` exits 0

Feedback latency (accepted on purpose): each task's first automated check -- one
`gh api` read or a `grep` -- is its fast signal, well under 30 s. The trailing
`make verify` (~42 s) stays: L13 makes it the one gate, and the pre-commit hook already
runs it on every commit, so it adds no wait the commit has not paid.
</verification>

<success_criteria>
ROADMAP SC-5 holds: a ruleset on `main` requires `test (3.12)`, `vendor-bundle` and
`image` green on an up-to-date head and requires a pull request, read back via
`gh api repos/halfb00t/spur/rules/branches/main`, and section 8 records the command that
applied it (D-12). ROADMAP SC-2's "documented merge path" and the L22 half of SC-4 hold:
`make pr.land PR=N` is what `docs/HOW_TO_DEVELOP.md` section 8 tells the human to run,
and L22 records the merge gate, the wall included.
</success_criteria>

<output>
Create `.planning/phases/05-ci-observed-green/05-03-SUMMARY.md` when done.
</output>
